# Pre-Phase 6 — Production Data Finalization

Status: **COMPLETE (2026-09-05)** — repository data is finalized and verified; Phase-6 PLANNING may start. No Phase-6 deployment or production provisioning was performed.

Authoritative decisions: `docs/phases/pre-phase-6/pre-phase-6-production-data-lock.md` + `production-inputs-manifest.json` (unchanged). This document is the verification/provenance record the lock requires.

## 1. Product decisions (recorded, NOT changed)

- **Admin emails** (configuration values for the Phase-6 deployment plan, not secrets):
  - `tee.johnlor@gmail.com`
  - `leaderboardwordle@gmail.com`
  - The runtime gate stays `env.ADMIN_EMAIL` in the existing bootstrap (`src/server/middleware/auth.ts`); Phase-6 deployment must configure these two addresses.
- **Leaderboard qualification thresholds** — already implemented, verified unchanged:
  - `WEEKLY_QUALIFICATION_COMPLETED_DAYS = 3` (`src/server/leaderboard/constants.ts:18`)
  - `MONTHLY_QUALIFICATION_COMPLETED_DAYS = 8` (`src/server/leaderboard/constants.ts:21`)
  - Semantics unchanged: only COMPLETED games on finalized eligible days; FAILED/FORFEITED/MISSED and today do not count.

## 2. Valid-guess dictionary (Task A) — 12,972 words

| Field | Value |
|---|---|
| Upstream source | `deedy/wordle-solver` → `data/official_wordle_all.txt` (original/pre-NYT Wordle guess list, extracted from the original Wordle site source `main.c1506a22.js`) |
| Pinned revision | commit `33a13bfe28b8f860f4d33a7c4c822892b0f8afef` (2022-01-20; last commit touching the file on `main` at import time) |
| Byte pin | SHA-256 `af8494946166f8083e605d6d2b37b814a80fda8f9e23069dab072c744326ee21` — bytes re-downloaded AT the pinned commit and verified identical to `main` |
| License | `deedy/wordle-solver` is MIT; the word list itself derives from the original Wordle website source (Josh Wardle). Recorded as upstream provenance — **not** a claim that the word data is MIT-licensed. |
| Import date | 2026-09-05 |

Verification (all pass):

- **exactly 12,972 words** — `bun run word-list` prints `built 12972 words`; client JSON holds 12,972 word entries;
- **lowercase a-z only** — `grep -cv '^[a-z]{5}$'` = 0 on source; the pipeline (`parseWordList`) throws on violations;
- **exactly 5 letters** — same checks (regex is `^[a-z]{5}$`);
- **unique** — `sort | uniq -d` = 0 on source; pipeline enforces;
- **deterministic ordering** — source is ASCII-sorted; pipeline output sorted; regeneration byte-identical (`sha256sum` before/after `bun run word-list` = identical);
- **server/client parity** — one canonical source → `src/server/data/valid-guesses.generated.ts` (server dictionary) + `src/lib/shared/data/valid-guesses.json` (client bundle), both generated by the same run; parity pinned by `tests/unit/word-list-server.test.ts` and CI `git diff --exit-code`.

Files: `src/server/data/valid-guesses.source.txt` (canonical, provenance header added), `src/server/data/valid-guesses.generated.ts`, `src/lib/shared/data/valid-guesses.json` (regenerated).

## 3. Answer pool (Task B) — 2,315 words (PRIVATE)

| Field | Value |
|---|---|
| Upstream source | `deedy/wordle-solver` → `data/official_wordle_common.txt` (original/pre-NYT Wordle answer pool, same snapshot as the guess list) |
| Pinned revision | commit `924deba09cfe0bbe285e80cddbd7a7bc6cc1f1b4` (2022-01-20; last commit touching the file on `main` at import time) |
| Byte pin | SHA-256 `ecc0269bce8250738f277c63103ed81a0d9904549a6d6da2c7cd6d32cca401f0` — verified identical at the pinned commit |
| License | same caveat as §2 (upstream MIT does not make the word data MIT) |
| Import date | 2026-09-05 (source placed; `bun run seed:answers` still to be run per target DB) |

Placement: `scripts/seed/answer-pool.source.txt` — **gitignored (`scripts/seed/*.txt`), never committed**, inside the existing private seed pipeline (`scripts/seed/import-answer-pool.ts`, `bun run seed:answers`). It did **not** enter any public artifact or client bundle (see §6).

Mechanical proofs:

- **2,315 answers** — source file word count 2,315; duplicates 0 (`uniq -d`); malformed 0 (`grep -cv '^[a-z]{5}$'` = 0);
- **`2315 / 2315 answers are present in the 12972 guess set`** — two independent checks:
  1. the project's own parser+set check (`parseAnswerPool` against `VALID_GUESS_SET`): `bun run test:unit tests/unit/answer-pool-import.test.ts` → 6/6 passed (throws on ANY missing member); the conditional pin reads the real seed file;
  2. independent set difference: `comm -23 <pool> <public guesses>` = **0 missing**; `comm -13` = 10,657 (guesses that are not answers — expected, guesses ⊃ answers).

## 4. Avatar allow-list (Task C) — 3,944 emoji

| Field | Value |
|---|---|
| Policy | All standard Unicode RGI emoji corresponding to Discord's standard/default emoji; Discord custom/server emoji excluded; Unicode sequences stored, no artwork (lock §4) |
| Unicode baseline | **Unicode Emoji 17.0 / Unicode 17.0.0** |
| Authoritative data | `https://www.unicode.org/Public/17.0.0/emoji/emoji-test.txt` — header `Version: 17.0`, `Date: 2025-08-04` |
| Selection rules | 1) every **fully-qualified** RGI sequence; 2) standalone components (skin-tone modifiers, keycap/tag parts) excluded — they are not themselves RGI emoji; 3) no Discord custom/animated-server emoji; 4) stored value = the Unicode sequence string; 5) deterministic ordering = ascending Unicode codepoint sequence; 6) labels = CLDR short names from the same file |
| Count | **3,944** fully-qualified RGI sequences (measured; the file's own fully-qualified count) |
| Import date | 2026-09-05 |

Files: `src/server/data/avatar-emojis.ts` (canonical server allow-list, replaced — provenance + rules in header), `src/lib/shared/config/avatar-emojis.generated.ts` (client twin, regenerated with the existing `bun run avatar-list` pipeline — byte-identical on regeneration, parity pinned by `tests/unit/avatar-list.test.ts` + CI diff gate).

The former 24-item curated set (D4) is **superseded** — recorded in the decision log (`docs/contradictions-and-gaps.md`, "Avatar set (D4 reproducibility)" row). Tests were updated from the old 20–30 pin to the exact 3,944 pin and to RGI-correct accept/reject cases (`👍🏽`, `🧑💻` accepted; standalone `🏽`, unqualified `©`, and non-RGI `🦊🏽` rejected).

## 5. Architecture preservation (Task D)

No redesign of auth, authorization, DB schema, game/leaderboard logic, scheduling, Phase-5 CSP/rate-limiting/security controls, or CI invariants. All existing pipelines (`bun run word-list`, `bun run avatar-list`, seed import, `verify:bundle`) are used unchanged. The only non-data source changes are test pins/comments forced by the real dataset sizes (see §7) and the decision-log records.

## 6. Secrecy

- The answer pool exists ONLY as `scripts/seed/answer-pool.source.txt` (gitignored) — it is not in `git status`, not in any committed artifact, and not in any client/server source.
- `bun run verify:bundle` (post-build): **0 non-public pool word(s) in 119 build files** — all 2,315 pool words are members of the PUBLIC guesses list, so they are expected in the bundle by design (documented verify:bundle rule, contradictions log 2026-08-30); the meaningful scan target (non-public pool words) is empty.
- Admin-plane static-embed pin (see §7) green.

## 7. Test/gate changes (scoped, no gate weakened)

| Change | Reason |
|---|---|
| `tests/unit/avatar-list.test.ts` | Old 20–30 count pin and "🙂/😀 rejected" cases assumed the 24-item curated set; new exact pin 3,944 + RGI-correct accept/reject |
| `tests/unit/profile-service.test.ts`, `tests/integration/profile.test.ts` | Same stale "non-curated" cases (`🙂`, `😀` are now valid RGI) → replaced with genuinely non-RGI strings (`🏽`, `©`, `🦊🏽`) |
| `tests/unit/admin-secrecy.test.ts` | Per-word quoted scan was designed for a 2-word fixture; with the real 2,315-word pool it false-positives on prose (measured ceiling: 2 distinct quoted pool words/file). Now a static-EMBED detector: fail at ≥25 distinct quoted pool words per file (embedded data = hundreds/thousands). Same protected invariant; deviation recorded in `docs/contradictions-and-gaps.md` 2026-09-05 |
| `scripts/build-avatar-list.ts` (header comment only) | Rendered client header described the old 24-entry set |
| `scripts/seed/README.md` | Provenance table filled with the real pool source/revision/hash |

## 8. Commands run (receipts)

```bash
# import + regenerate
bun run word-list        # built 12972 words (client JSON + server TS)
bun run avatar-list      # built 3944 avatars (client twin)
# parity/determinism
sha256sum -c <hashes-before>  # OK — regeneration byte-identical (word-list + avatar-list)
# static checks
bun run lint             # clean (eslint .)
bun run check            # svelte-check: 0 errors, 0 warnings
bun run test:unit        # 33 files passed, 233 tests passed (8 files/89 skipped conditional)
bun run test:unit tests/unit/answer-pool-import.test.ts  # 6/6 — answers ⊂ guesses pin
bun run build            # production build OK
bun run verify:bundle    # bundle secrecy OK — 0 non-public pool word(s) in 119 build files
```

NOT run locally (CI-gated; need the non-production `DATABASE_URL`): `bun run test:integration`, `bun run test:e2e`. Their source was updated for the avatar policy where needed (§7) and will run in CI.

## 9. Unresolved issues / Phase-6 notes

- `scripts/seed/answer-pool.source.txt` must be seeded into each target (non-production first) DB with `DATABASE_URL=... bun run seed:answers` — not run here (no DB).
- Phase-6 deployment configuration must set `ADMIN_EMAIL` to BOTH `tee.johnlor@gmail.com` and `leaderboardwordle@gmail.com` per the existing promotion semantics (bootstrap promotes each listed address; see `src/server/middleware/auth.ts`).
- Avatar picker UI now renders ~3,944 choices (product decision; a search/filter or grouped layout may be a Phase-6 UX consideration — NOT changed here).
- The deedy/wordle-solver answers file (`official_wordle_common.txt`) is the pre-NYT pool; the project's answer scheduling reads the DB `answer_dictionary` — no code change needed.