# Leaderboard Wordle — Contradictions and Gaps

Cross-referencing Architecture-v3 against the proposed repository tree, dependency list, indexed documentation, and gpt v6–v9 reviews, plus independent audit and Luna v10–v16 cross-checks (2026-08-23) that add the open items NC1–NC3, NG1–NG25, M1–M5 below, including findings from the bounded SvelteFlare reference audit (`pinebasedev/svelteflare@9a0a2dd`).

## Resolved contradictions (fixed in Architecture-v3 + Specifications-v1)

| # | Issue | Resolution |
|---|---|---|
| ~~C1~~ | Phase ordering | Resolved: rewritten as vertical slices (Phase 0-6). |
| ~~C2~~ | FSD premature layers | Resolved: tree simplified to `app/` + `shared/` only. Future structure labeled as extracted, not initial. |
| ~~C3~~ | Security in wrong phase | Resolved: Phase 5 reworded as "verification/hardening gate." |
| ~~C4~~ | Phase 1 depends on authentication | Resolved: Better Auth moved to Phase 0. |
| ~~C5~~ | "Locked" vs current-day leaderboard | Resolved: spec updated "until the puzzle locks" → "until the puzzle finalizes." |
| ~~C6~~ | Neon HTTP vs interactive transactions | Resolved: switched to WebSocket-capable driver. |
| ~~C7~~ | `SCHEDULED → ACTIVE` no transition | Resolved: added `activatePuzzle()`. Midnight order fixed: finalize first, then activate. |

## Resolved gaps (fixed in Architecture-v3 + Specifications-v1)

| # | Issue | Resolution |
|---|---|---|
| ~~G1~~ | locked_at clarification | Resolved: explicit mutability state note. |
| ~~G2~~ | Daily settlement execution | Resolved: Cron Trigger + idempotent operations. |
| ~~G3~~ | MISSED persistence | Resolved: derived (absence of game row). |
| ~~G4~~ | Game concurrency | Resolved: UNIQUE constraints + transactions. |
| ~~G5~~ | Game start idempotency | Resolved: UNIQUE constraint + returns existing. |
| ~~G6~~ | Answer lock race | Resolved: `WHERE locked_at IS NULL` guard. |
| ~~G7~~ | Leaderboard aggregation | Resolved: `dailyScore()`, `leaderboard_guess_count`, threshold. |
| ~~G8~~ | Onboarding state | Resolved: `onboarding_completed_at` + invariant. |
| ~~G9~~ | Display-name uniqueness | Resolved: `display_name_normalized` + UNIQUE. |
| ~~G10~~ | Test architecture | Resolved: `tests/unit/`, `integration/`, `e2e/`, `security/`. |
| ~~G11~~ | Hono RPC type boundary | Resolved: type-only imports, runtime forbidden. |
| ~~G12~~ | Leaderboard guess count | Resolved: `COMPLETED ? guess_count : 6`. |
| ~~G13~~ | Participation threshold | Resolved: absolute days, finalized only. |
| ~~G14~~ | Tree missing hooks.server.ts | Resolved: added to tree. |
| ~~G15~~ | Valid-guess dictionary source | Resolved: canonical source → server + client. |
| ~~G16~~ | Answer pool deployment | Resolved: private source → seed → Neon. |
| ~~G17~~ | Finalization transaction | Resolved: atomic, idempotent, independently retryable. |
| ~~G18~~ | Clock/late-request | Resolved: PostgreSQL database time for all timestamps. |
| ~~G19~~ | Client request-ordering | Resolved: one guess in flight per game. |
| ~~G20~~ | Admin bootstrap | Resolved: `ADMIN_EMAIL` + atomic promote, never demotes. |
| ~~G21~~ | Missing-puzzle behavior | Resolved: hard invariant, fail closed + alert. |
| ~~G22~~ | Duplicate answer not DB-enforced | Resolved: `UNIQUE(answer_id)` constraint. |
| ~~G23~~ | Leaderboard rank not deterministic | Resolved: third tiebreaker + dense rank. |
| ~~G24~~ | Timestamp authority split | Resolved: PostgreSQL database time unified. |
| ~~G25~~ | Rate limiting | Resolved: Cloudflare Workers Rate Limiting API specified. |

## Resolved from gpt v8 review

| # | Issue | Resolution |
|---|---|---|
| ~~N1~~ | Rate limiting "in-request counters" | Resolved: replaced with Cloudflare Workers Rate Limiting API. |
| ~~N2~~ | Rate limit mechanism underspecified | Resolved: Cloudflare Rate Limiting binding in wrangler.toml. |
| ~~N3~~ | Midnight operation failure isolation | Resolved: finalize before activate; each independently retryable. |
| ~~N4~~ | Admin bootstrap in Phase 2 and Phase 4 | Resolved: removed from Phase 4, kept in Phase 2. |
| ~~N5~~ | Phase 3 "persistent results" misleading | Resolved: reworded to "history/statistics over persisted game results." |
| ~~N6~~ | Tiebreaker needs intentional note | Resolved: explicit speedrun-app rationale added. |
| ~~N7~~ | Dense rank terminology error | Resolved: "next rank skips" → "next distinct result receives immediately following rank." |
| ~~N8~~ | Spec still has LOCKED lifecycle | Resolved: replaced with `SCHEDULED→ACTIVE→FINALIZED` + mutability via `locked_at`. |
| ~~N9~~ | "Active horizon" undefined | Resolved: concept eliminated; invariant reworded non-tautologically (see N15). |

## Design questions

| # | Issue | Resolution | Status |
|---|---|---|---|
| ~~D1~~ | completed_count denormalization | Removed from V1 schema. Derive via COUNT. | Resolved |
| ~~D2~~ | TanStack Query vs local state | TanStack Query for durable server state. Local Svelte for ephemeral. | Resolved |

## Resolved from gpt v9 review

| # | Issue | Resolution |
|---|---|---|
| ~~N10~~ | Guess submission vs finalization transaction race | Resolved: puzzle row is serialization point; guess/game transactions lock puzzle first. |
| ~~N11~~ | Game start and locked_at need one transaction | Resolved: game start = answer locking in same transaction (lock puzzle → set locked_at → create/retrieve game). |
| ~~N12~~ | answer_dictionary.word not uniquely constrained | Resolved: added UNIQUE(word) + UNIQUE(normalized_word) constraints. |
| ~~N13~~ | No enforced approved answers ⊂ valid guesses | Resolved: seed/import process must verify every answer exists in valid-guess set. |
| ~~N14~~ | Pre-game API does not guarantee hint secrecy | Resolved: GET /api/game/today returns no hint pre-start; POST /api/game/start returns hint + game state. |
| ~~N15~~ | "Active horizon" definition is tautological | Resolved: "A puzzle must exist before a date becomes the next active date." |
| ~~N16~~ | Cron recovery after missed executions underspecified | Resolved: cron is reconciliation job — finalize expired, activate today, alert if missing. |
| ~~U1~~ | proposed-dependencies.md stale about Neon | Resolved: updated to "WebSocket-capable driver for interactive transactions." |

## Open items — audit + Luna v10 cross-check (2026-08-23)

Independent audit of this file against Architecture-v3, Specifications-v1, the proposed tree/dependencies, and Luna v1–v9, cross-checked by Luna v10. All previously listed items stay resolved. The items below are open and tracked; **none is critical-blocking**. "Fix" gives the proposed solution to fold into the docs/implementation when that phase starts. Per Luna v10: the NG6 and NG10 fixes were **replaced**, other fixes were tightened, NG9's boundary rule was sharpened (see row), and NG12–NG18 were added. Luna v11 confirmed the NG9 deadline contract and mandated the two lock-order concurrency tests (see NG9 row). Luna v12 confirmed the remaining items; the NG8/NG15 recovery rule was rewritten as an atomic same-day replacement per its review (see rows). On 2026-08-23 (Luna v13) the accepted decisions were propagated into `Architecture-v3.md`, `Specifications-v1.md`, `proposed-repo-tree.md`, and `proposed-dependencies.md`; the NC1 wording and the rate-limit enforcement-column residual listed below are now fixed in the source documents. Luna v14–v16 (bounded SvelteFlare audit, 2026-08-23): NG4's mechanism was sharpened (custom JSON Origin/Sec-Fetch-Site middleware — Hono's built-in `csrf()` is gated to form content-types and cannot protect a JSON client), `src/server/middleware/` gained an explicit home, and NG19–NG21 plus an investigation backlog were added from the audit. Final re-audit (2026-08-23, close of the review chain): all `Location` citations were converted from line numbers to **section references** (line numbers drift on every edit), the M3 eligibility anchor was aligned with the NG9 deadline contract (`transaction_timestamp()`), and NG22–NG25 were added (secure-headers baseline, CI pipeline, `completion_time_ms` stored-vs-derived, duplicated session/cookie sections).

### Doc-alignment contradictions (NC)

| # | Issue | Severity | Location | Fix | Resolve in |
|---|---|---|---|---|---|
| **NC1** | `MISSED` still described as a stored row | Medium | Specifications-v1 §10–§11 vs Architecture-v3 §games (MISSED derived) | Spec §10–§11: `MISSED` is a derived state (absence of a game row for a finalized puzzle), not a stored status; keep `guess_count = 0` only as the aggregation placeholder for leaderboard math. | Docs — now |
| **NC2** | `display_name_normalized` / `onboarding_completed_at` absent from Better Auth schema blocks | Medium | Architecture-v3 §user (Better Auth managed) — both schema blocks | Add both columns to the canonical `user` schema block in both places as application extensions via Better Auth `user.additionalFields`, AND define the same columns in the Drizzle schema passed to the adapter — both kept in sync through migrations (config alone does not create columns). | Phase 0 (schema) |
| **NC3** | Word-list / profanity data versioning unrecorded | Medium | Architecture-v3 §Word-data model; proposed-dependencies §packages-not-named | Record concrete file paths + version discipline for the canonical valid-guess list, the approved-answer pool, and the banned-word list (concrete paths in NG6/NG7). | Phase 0 (decision) |

### Open gaps (NG)

| # | Issue | Severity | Location | Fix | Resolve in |
|---|---|---|---|---|---|
| **NG1** | `Asia/Manila` midnight cron has no UTC expression | High | Architecture-v3 §Settlement / §daily_puzzles | Cloudflare Cron is UTC-only: `triggers.crons = ["0 16 * * *"]` (Manila = UTC+8, no DST). Define `puzzle_date AS DATE` and `expires_at TIMESTAMPTZ = (puzzle_date + 1) AT TIME ZONE 'Asia/Manila'`, computed at schedule/activation; settlement compares `expires_at <= now()` in the DB. | Phase 0 (wrangler + schema) |
| **NG2** | Hint letter not validated | High | Architecture-v3 §daily_puzzles (hint_letter) / §Admin answer validation | At schedule time verify `hint_letter` is exactly one ASCII letter AND occurs in the answer (a DB CHECK cannot enforce cross-row membership); DB CHECK only for shape: `char_length(hint_letter) = 1 AND hint_letter ~ '^[A-Z]$'`; generate and persist the hint **at scheduling time**, not at activation. | Phase 0 (CHECK) + Phase 4 (schedule validation) |
| **NG3** | `puzzle_date`/`expires_at` types and leaderboard indexes undefined | High | Architecture-v3 §daily_puzzles / §Settlement | `puzzle_date DATE NOT NULL UNIQUE`; `expires_at TIMESTAMPTZ NOT NULL` (per NG1). Indexes are candidates, not final: derive from actual queries (today's puzzle, expired non-finalized, games by puzzle/user, completed games per puzzle); UNIQUE constraints already create indexes — confirm `daily_puzzles(status, puzzle_date)` and `games(puzzle_id, status)` against query plans. | Phase 0 (first migration) |
| **NG4** | CSRF policy for Hono cookie mutations | Medium | Architecture-v3 §CSRF boundary / §Session/cookie strategy | SameSite=Lax already blocks cross-site POST cookies. Add a **custom** Hono middleware (in `src/server/middleware/`) verifying `Origin`/`Sec-Fetch-Site` on **all unsafe application cookie-authenticated mutations regardless of content-type** (`/api/game/*`, `/api/me/*`, `/api/admin/*`). Do NOT rely on Hono's built-in `csrf()`: it gates only form content-types (urlencoded/multipart/text-plain) and never fires for JSON requests, so it cannot protect the `hc` JSON client. Exclude `/api/auth/*` (OAuth callbacks are cross-site by nature; Better Auth owns its CSRF there). `Sec-Fetch-Site` is auxiliary — reject only when BOTH origin and sec-fetch-site checks fail. Audit that no state-changing endpoint is GET-reachable (incl. Better Auth sign-out). | Phase 0 (middleware baseline) |
| **NG5** | Theme (light/dark) persistence undefined | Medium | Specifications-v1 §2 / Architecture-v3 §Styling/UI | Persist in `localStorage` (`theme` key); apply before first paint via `src/lib/app/theme` provider (inline head script) to avoid FOUC; default from `prefers-color-scheme`; no SSR/DB dependency. | Phase 2 |
| **NG6** | Display-name normalization vs moderation pipeline vague | Medium | Specifications-v1 §1, §15 / Architecture-v3 §Display-name rules | Two distinct functions: `canonicalizeDisplayName()` for the `display_name_normalized` UNIQUE column (ASCII charset, lowercase, trim, collapse whitespace) and `moderationKeyForDisplayName()` for profanity/obfuscation detection (aggressive: leet/confusable mapping, separator removal). **Superseded-location note (Phase-2 Decision "Display name rules", 2026-08-25):** the server-authoritative implementation now lives in `src/server/profile/display-name.ts`, mirrored for the client in `src/lib/shared/lib/display-name.ts`, with table-driven/property-based parity tests across the five behaviors (charset, whitespace normalization, canonicalization, moderation transformation, reserved names) — this supersedes the earlier shared-only location proposal (`src/lib/shared/lib/` as the single home). Pin the V1 display-name charset to ASCII explicitly (`[a-z0-9 _-]`, case-insensitive) instead of vague "letters"; baseline + custom banned list in a versioned JSON file under `src/lib/shared/config/`. | Phase 2 |
| **NG7** | Valid-guess dictionary has no tree location | Medium | Architecture-v3 §Valid-guess dictionary / proposed-repo-tree §Directory structure | Add the paths to BOTH the architecture references and `docs/proposed-repo-tree.md` (else the tree's own "no invented files" rule is violated): `src/server/data/valid-guesses.source.txt` (git, canonical), generated `src/lib/shared/data/valid-guesses.json` (client bundle), `scripts/build-word-list.ts` (one canonical source → server + client artifacts with an automated equality/version build check per Architecture-v3 §Valid-guess dictionary); private gitignored answer file + `scripts/seed/` import (provenance per NG16). | Phase 0–1 (build pipeline) |
| **NG8** | Admin delete/edit of SCHEDULED puzzles undefined | Medium | Architecture-v3 §Admin scheduling window / Specifications-v1 §16 | DELETE allowed only when `locked_at IS NULL AND status = 'SCHEDULED' AND puzzle_date > current Asia/Manila date` (future dates), else 403 — today's SCHEDULED puzzle cannot be plain-deleted; content changes for today go through the atomic same-day replacement only (NG15). Ordinary scheduling/editing applies to future dates; past dates rejected. Moving a scheduled puzzle recomputes `expires_at` and re-checks `UNIQUE(puzzle_date)`; resulting date gaps trigger the missing-puzzle alert. | Phase 4 |
| **NG9** | Expiry-check snapshot vs commit (TOCTOU) | Low–Medium | Architecture-v3 §Expiry deadline contract / §Concurrency | Deadline contract (confirmed by Luna v11): a mutation is eligible only when its transaction begins before `expires_at` — `transaction_timestamp()` is the authoritative **transaction-start eligibility anchor** (not a proxy for HTTP arrival; `statement_timestamp()` is server-receipt time). After eligibility, the puzzle-row lock is the serialization point against `finalizePuzzle`: whichever acquires it first decides the outcome. Do NOT use `clock_timestamp()` as the authority: it shares the same check-then-commit gap and would make acceptance depend on lock-wait timing. Mandatory midnight-crossing integration tests, both lock orders: (A) guess locks first → commits COMPLETED, finalize converts only remaining ACTIVE games; (B) finalize locks first → guess acquires the lock post-finalization, its post-lock `status = ACTIVE` check fails (observes `FINALIZED`), guess rejected. Invariant: a request whose transaction-start eligibility time is after `expires_at` can never pass the expiry check. | Phase 0–1 (transaction semantics) |
| **NG10** | Google name / `emailVerified` behavior undefined | Low | Architecture-v3 §Onboarding state / §Auth (Better Auth) | Do not hard-code `emailVerified = true`: Better Auth maps the provider's verification signal into `emailVerified` (per docs, "for social logins, email verification status is read from the SSO"); enable per-provider `requireEmailVerification: true` for Google (defaults `false`; the gate checks the local user's verification state and redirects with `email_not_verified` when unverified). Google `name` may prefill `user.name`, but onboarding re-validates against display-name rules (2–15 chars, ASCII charset) and forces a new choice if invalid; `display_name_normalized` computed at onboarding save. | Phase 0–2 |
| **NG11** | Top-10 vs full leaderboard response undefined | Low | Specifications-v1 §12 / Architecture-v3 §Core API shape | Leaderboard endpoints take `?limit=10`; define `limit` as a dense-rank cutoff (`rank <= 10` — may return more than 10 rows) and `count` as the total number of qualified players (`entries` = rows through the cutoff; for the viewer's own rank see NG13). | Phase 3 |
| **NG12** | "Earliest qualifying completion" tiebreaker undefined | Medium | Architecture-v3 §Ranking model (final tiebreaker) | Define `earliest_qualifying_completion_at` = min(`completed_at`) over the player's COMPLETED games on eligible finalized days in the period — the same day set used for the score average — or explicitly choose a different rule. | Phase 3 |
| **NG13** | Current user's rank unavailable when outside top-N | Medium | Specifications-v1 §12–§13 / Architecture-v3 §Core API shape | Include the viewer's own rank in leaderboard responses: `{ entries, count, currentUser: { rank, entry } }` (or a separate rank endpoint) so the result screen can show "Current position" at any rank. | Phase 3 |
| **NG14** | Rank key vs display-order key must stay separate | Low | Architecture-v3 §Ranking model (rank vs display order) | Same fix as M2: `user_id` only in the final display `ORDER BY`, never inside the `DENSE_RANK()` window — listed separately so the separation is explicitly test-covered. | Phase 3 |
| **NG15** | Admin mutation rules at the Asia/Manila day boundary | Medium | Architecture-v3 §Admin scheduling window / Specifications-v1 §16 | Explicit state rule, not a side effect of DELETE guards: once a puzzle's effective date begins, its answer/hint is immutable. Lifecycle: future date → normal scheduling/editing; today + `SCHEDULED` + never started (cron missed) → **atomic same-day replacement** only; today + `ACTIVE` → immutable; `FINALIZED` → immutable. The replacement is a single recovery transaction (not delete+reschedule): lock the row, verify `puzzle_date` = current Asia/Manila date, `status = 'SCHEDULED'`, `locked_at IS NULL`, then UPDATE `answer_id`/`hint_letter` in place (re-checks `UNIQUE(answer_id)`, regenerates/persists the hint per NG2, recomputes `expires_at`) — no transient gap, no spurious missing-puzzle alert. | Phase 4 |
| **NG16** | Word-list provenance/reproducibility | Medium | Architecture-v3 §Valid-guess dictionary / tree §Data files | Record for each list (valid guesses, answer pool, banned words): upstream source, exact version/commit, license, import date, normalization and five-letter filtering rules; automated build check that server/client artifacts agree and answers ⊂ valid guesses. | Phase 0–1 |
| **NG17** | Pre-paint theme script vs CSP | Low | Specifications-v1 §2 / Architecture-v3 §Styling/UI (theme) | NG5's inline head script must remain CSP-compatible (nonce or script hash) when the security-hardening phase adds a Content-Security-Policy. | Phase 2/5 |
| **NG18** | Admin-bootstrap identity assumptions | Low | Architecture-v3 §Admin bootstrap | Document as a security decision: admin promotion keys on the verified email (enable per-provider `requireEmailVerification` for Google so `emailVerified` gates the bootstrap). With Google as the only provider in V1, implicit account linking applies only to same-email verified identities, and linking never mutates the local `email`/`emailVerified` (Better Auth `accountLinking`). Misconfigured `ADMIN_EMAIL` → no admin → recovery is a **manual database/operator bootstrap** (SQL/migration procedure, verified by the operator) — a no-admin state cannot be fixed through the app Admin UI. | Phase 0/2 |
| **NG19** | Request timeout undefined | Low | Architecture-v3 §Middleware; SvelteFlare `apps/api/src/middleware/timeout.ts` [observed] | Wrap the composed Hono app with `hono/timeout` (30 s) returning a JSON 408 using the NG21 error envelope; zero-coupling addition to `src/server/middleware/`. | Phase 0 |
| **NG20** | Payload-size enforcement undefined | Low | Architecture-v3 §Middleware; Specifications-v1 §21 | Enforce a 64 KB request-body cap → JSON 413 with the NG21 error envelope, applied before validation. | Phase 0 |
| **NG21** | Error envelope + requestId undefined | Low | Architecture-v3 §Error envelope contract; SvelteFlare `apps/api/src/helpers/http.ts` [observed] | Central error contract: every API error returns `{ error: { code, message, requestId, issues? } }` with a stable status→code map; centralized `onError`/`notFound`; one `requestId` per request echoed in responses and logs for correlation; log bodies only for status ≥ 500. | Phase 0 |
| **NG22** | Secure-headers baseline undefined | Low | Architecture-v3 §Security model (middleware lists "secure headers" without defining them); NG17 | Define the baseline header set centrally in `src/server/middleware/`: `X-Content-Type-Options: nosniff`, framing policy (`frame-ancestors 'none'`, or `X-Frame-Options: DENY` until CSP lands), `Referrer-Policy: strict-origin-when-cross-origin`, HSTS in production; CSP with nonce/hash per NG17 hardened at Phase 5. | Phase 0 scaffold + Phase 5 harden |
| **NG23** | CI pipeline undefined | Low | Architecture-v3 §Test architecture / §Development phases; tree has no CI path | Add `.github/workflows/ci.yml`: `bun install` → `bun check`/lint → unit + integration tests (Neon branch DB or local Postgres — never production) → build; Playwright e2e/security job on PR or nightly. Add the path to the tree. | Phase 0–1 |
| **NG24** | `completion_time_ms` stored vs derived ambiguous | Low | Architecture-v3 §games (column comment) vs Specifications-v1 §10 ("stores") | Decide and document: compute once in the completion transaction and **store** it (frozen; matches Spec §10 and the raw-facts principle), or compute on read. Recommended: stored column set at completion; update the schema comment. | Phase 0 (schema) |
| **NG25** | Session/cookie strategy duplicated in Architecture-v3 | Low | Architecture-v3 §Session/cookie strategy (two sections) | Merge into one section (keep the unique host-only-cookie, no-JS-visible-token, and no-token-storage details); single source prevents drift. | Docs — now |

### Additional gaps (M) — independent audit findings

| # | Issue | Severity | Location | Fix | Resolve in |
|---|---|---|---|---|---|
| **M1** | Week-start day undefined (Monday vs Sunday) | Medium | Specifications-v1 §12 (This week) | Define the canonical week start as a product constant (recommend **Monday**, ISO-8601, in the Asia/Manila calendar) and use it for "This week" boundaries. | Decision now, Phase 3 |
| **M2** | No final deterministic tiebreak | Low | Architecture-v3 §Ranking model | Keep two ordering concepts separate: rank key `DENSE_RANK() OVER (ORDER BY avg_time, avg_guesses, earliest_completion)`; display order `ORDER BY rank, user_id`. Never include `user_id` in the rank window — it must not change shared ties (see NG14). | Phase 3 |
| **M3** | Lazy finalization exists, lazy activation does not | Medium | Architecture-v3 §Settlement (lazy paths) | In `POST /api/game/start`, lazily activate today's SCHEDULED puzzle in the same transaction if the cron missed it, only when all guards hold — after acquiring the puzzle-row lock (`SELECT ... FOR UPDATE`) so cron activation and lazy activation serialize on the same row: `puzzle_date` = current Asia/Manila date, `status = 'SCHEDULED'`, `expires_at > transaction_timestamp()` (NG9 anchor), and no other ACTIVE puzzle for today's date. Fail-closed + alert only when the row is absent (missing-puzzle invariant). | Phase 0–1 |
| **M4** | Avatar-emoji file path contradicts tree/FSD | Low–Medium | Specifications-v1 §15 vs proposed-repo-tree §shared/config | **Superseded by the Phase-2 Decision "Avatar" (2026-08-25).** Phase-2 resolution: canonical SERVER-owned list `src/server/data/avatar-emojis.ts` → generated client artifact `src/lib/shared/config/avatar-emojis.generated.ts` via `scripts/build-avatar-list.ts` (parity-tested, CI diff-gated). The server list is authoritative; the generated client file is never hand-edited. This supersedes the earlier proposed-tree path `src/lib/shared/config/avatar-emojis.ts`. | Phase 2 (decided) |
| **M5** | Scheduling window and date-move semantics undefined | Low–Medium | Specifications-v1 §16 / Architecture-v3 §Admin scheduling window | Same boundary semantics as NG8/NG15: past-date rejection, day-boundary immutability, atomic same-day replacement (recovery), date-move `expires_at` recomputation, DELETE guard (future dates only), gap alert. | Phase 4 |

## Investigation backlog (SvelteFlare audit 2026-08-23)

Not open items — recorded so decisions are deliberate, not accidental:

- Better Auth `cookieCache` option (SvelteFlare `apps/api/src/auth.ts`) — 5-minute session staleness vs revocation semantics; assess against ASVS at Phase 0/5. A Google-only setup may not need it.
- `wrangler types`-generated binding types vs `@cloudflare/workers-types` — decide at Phase 0 (bindings shape affects `c.env` typing through the Hono bridge).

## Proposed tree compliance

| Rule | Tree compliance | Status |
|---|---|---|
| FSD applies to `src/lib` only | ✅ | ✅ |
| Hono backend in `src/server/` | ✅ | ✅ |
| Bridge is integration-only | ✅ | ✅ |
| No FSD in `src/routes` | ✅ | ✅ |
| `src/lib/shared/auth/` removed | ✅ | ✅ |
| `src/server/auth/` is sole auth location | ✅ | ✅ |
| `src/app.d.ts` added | ✅ | ✅ |
| `src/hooks.server.ts` in tree | ✅ | ✅ |
| Test directories in tree | ✅ | ✅ |
| FSD layers minimal | ✅ | ✅ |

## Summary

- **7 contradictions** — all resolved
- **25 gaps** — all resolved
- **9 issues from gpt v8** — all resolved
- **8 issues from gpt v9** — all resolved (N10–N16 + U1)
- **2 design questions** — all resolved
- **0 critical blocking items**
- **33 open items tracked** — 3 doc-alignments (NC1–NC3), 25 gaps (NG1–NG25), 5 audit findings (M1–M5) — none blocking; resolve in the owning phase listed above. NG19–NG23 came from the SvelteFlare audit and the final re-audit; NG24–NG25 are docs/schema-clarity items. Decide before Phase 0 scaffolding ends: NG1, NG3, NG4, NC2, M4, NG19–NG23 (schema/cron/middleware/CI shape).
- **Wording residuals: resolved by the 2026-08-23 propagation (Luna v13)** — NC1's Spec §10–§11 `MISSED` wording and the Architecture-v3 rate-limit "Enforcement" column are now fixed in the source documents. Remaining caveat: "Proposed tree compliance" is vs the *proposed* tree until code exists.
- **10/10 proposed tree compliance rules** — all ✅ (vs the proposed tree; re-verify against the implemented tree)

Architecture is ready for implementation planning; the open items above are tracked decisions with proposed fixes, not blockers.

## Phase 0 resolutions (2026-08-23 — implemented, code-first)

Phase 0 is complete: foundation + auth core + test/CI infrastructure. **External-gate status (clarified 2026-08-25):** Phase 0's original external gates were CLOSED at the B8 baseline (live non-prod Neon migration + WebSocket transaction proofs, real Google OAuth flow, GitHub Actions run green). Subsequent Phase-1 and post-Phase-1 mutations require RE-VERIFICATION of the latest Google OAuth round-trip and the latest GitHub Actions run — see the current Phase-2 implementation handoff (`phases/phase 2/phase-2-implementation-handoff.md`) for the exact remaining external checks. Status of the tracked items:

| Item | Phase-0 status |
|---|---|
| NG1 (Manila cron/UTC) | ✅ In code: `wrangler.toml` `[triggers] crons = ["0 16 * * *"]`; `puzzle_date` DATE + `expires_at` TIMESTAMPTZ in migration `0000_init` |
| NG2 (hint validation) | ✅ Shape CHECK `hint_letter_shape` in migration (integration-tested); membership-in-answer validation belongs to `src/server/puzzle` at scheduling time (Phase 4, per architecture) |
| NG3 (types/indexes) | ✅ Implemented in `src/server/db/schema.ts` + migration incl. the documented candidate indexes |
| NG4 (CSRF) | ✅ Custom Origin/Sec-Fetch-Site middleware (`src/server/middleware/csrf.ts`), `/api/auth/*` excluded; fail-closed (headerless + `Sec-Fetch-Site: none` rejected) |
| NG9 (TOCTOU/lock order) | ✅ Puzzle-row lock-first contract + both midnight lock-order integration tests (`tests/integration/midnight-lock-order.test.ts`) — green on Neon |
| NG19 (request timeout) | ✅ Hono timeout 30 s → JSON 408 envelope |
| NG20 (body limit) | ✅ 64 KB → JSON 413 envelope |
| NG21 (envelope/requestId) | ✅ `{ error: { code, message, requestId, issues? } }` + centralized onError/notFound; requestId middleware first |
| NG22 (secure headers) | ✅ nosniff / X-Frame-Options DENY / Referrer-Policy + HSTS over TLS |
| NG23 (CI) | ✅ `.github/workflows/ci.yml` — unit-and-build, integration (gated on non-prod `DATABASE_URL` secret), e2e; run green |
| NG24 (completion_time_ms) | ✅ Stored-once nullable column in `games` |
| NC2 (additionalFields sync) | ✅ `avatarEmoji`/`role`/`display_name_normalized`/`onboarding_completed_at` present identically in Better Auth config, generated schema, and migration |
| NC3 (word-list tooling/provenance) | ✅ `scripts/build-word-list.ts` (source → public artifact) + `scripts/verify-bundle-secrecy.ts` (client+server bundle scan); `scripts/seed/README.md` provenance rules; answer pool gitignored |
| M3 (lazy activation) | ✅ Contract test green (`tests/integration/lazy-activation.test.ts`) |
| M4 (avatar emojis path) | ⏳ Directory scaffolded (`src/lib/shared/config/`); the `avatar-emojis.ts` file lands with Phase 2 onboarding |
| Hono-side authentication helper (Phase-0 definition / §Auth ownership) | ✅ `src/server/middleware/auth.ts` — `authContext` (Better Auth session resolution from cookies/headers, cookie fast-path, never `event.locals`) + `requireAuth` (401 NG21 envelope) + typed `c.get('auth')`; mounted on `/api/game/*`, `/api/me/*`, `/api/admin/*`; `/api/auth/*` untouched; unit tests `tests/unit/hono-auth.test.ts` (7) |
| NG23 lint requirement (Phase-0 definition: lint in CI) | ✅ ESLint 9 flat config + `typescript-eslint` + `eslint-plugin-svelte` (`eslint.config.js`); `bun run lint`; CI step. svelte-check untouched |
| Better Auth CLI reproducibility | ✅ devDependency `auth@1.7.1` pinned (`bun.lock`); `auth:schema` uses the local binary; regeneration deterministic and byte-identical to the committed canonical file; `auth:check` is a CI gate |
| Google token-storage contradiction (§Session/cookie strategy "Do not store") | ✅ Resolved as a documented decision: token columns are Better Auth-managed core-schema fields (its 1.7.1 callback persists them unconditionally; removal breaks account creation + CLI regeneration). App requests OIDC scopes only, never `access_type=offline` → `refresh_token` stays NULL; no Google-API code in `src/`. Regression test: account columns exist and are nullable (integration). See Architecture-v3 §Session/cookie strategy + B8 |
| CI integration gate mandatory | ✅ `integration` job fails when the non-prod `DATABASE_URL` secret is missing (no silent skip); `lint` + `auth:check` steps added |

Post-Phase-0 recheck note (2026-08-23): Better Auth schema CLI regeneration was unpinned (`auth@latest`) and drifted (fingerprints only: `defaultNow()` markers + `@__PURE__` annotations). Guard added: `bun run auth:check`. **RESOLVED (2026-08-24, B8):** CLI pinned as devDependency `auth@1.7.1` (in `bun.lock`); `auth:schema` runs the local binary; regeneration is deterministic and byte-identical to the committed canonical file; `auth:check` is a CI gate. The fingerprint tolerant normalizer in `scripts/check-auth-schema.ts` is retained (any real drift still fails).

## Phase 1 resolutions (2026-08-24 — implemented, code-first)

Phase 1 (authenticated game vertical slice) is complete: server services,
Hono endpoints, typed RPC client, Tailwind/svelte-query UI, re-pointed
integration suites, authenticated E2E. Every decision below is recorded
because it either adds a dependency or clarifies architecture wording.

| Decision | Resolution |
|---|---|
| **Hono RPC typing requires chained registrations** | Hono accumulates its route Schema in the type of the chained return value (`.post(...)` mutates the same object but returns a new typed instance). `routes.ts` previously discarded every return, so `AppType` was schema-less and `hc<AppType>` resolved `unknown`. Fixed: the whole middleware/auth/game chain is now chained; `registerGameRoutes` returns the composed app and must never be annotated/cast (that erases the schema). Any future route registration must chain or the RPC client silently loses the endpoint. |
| **`@hono/zod-validator` 0.9.0 added** | hono 4.13.3 no longer bundles `hono/zod-validator` (it moved out of core). Peer range `hono >=4.11.2`, `zod ^3.25 || ^4` — verified against the registry. Needed so the RPC client's `json` input is typed (`$post({ json })`) instead of manual response declarations. Validation failures map to the NG21 `BAD_REQUEST` envelope via the hook; the malformed-JSON path (a raw `HTTPException(400)` that never reaches the hook) is mapped in `onErrorHandler` — a deliberate, sanitized extension of the "only 408 preserved verbatim" policy. |
| **Client timing fields: REJECTED, not ignored** | The guess body schema is `.strict()` — any client-supplied `startedAt`/`completedAt`/`completionTimeMs`/`status` field fails with 400. (Prompt §10 allowed either; rejection is the stronger posture and is unit-tested.) |
| **Endpoint names** | `POST /api/game/start`, `GET /api/game/current`, `POST /api/game/:gameId/guess` — contract documented in `phases/phase 1/phase-1-api.md`; Hono RPC types are the wire source of truth. |
| **Server dictionary artifact** | `scripts/build-word-list.ts` now also emits `src/server/data/valid-guesses.generated.ts` (`VALID_GUESS_SET` — authoritative server validation). Parity tests pin source ↔ server ↔ client equality (NG7 "server + client artifacts" finally implemented). |
| **E2E deterministic auth fixture** | `tests/e2e/helpers/auth-fixture.ts`: inserts a real user + session row on the non-production DB and constructs the exact Better Auth session cookie (Hono signed-cookie format `token.signature`, base64 HMAC-SHA256 — verified against better-auth 1.7.1 + hono 4.13.3). CI e2e job takes `DATABASE_URL` + `BETTER_AUTH_SECRET`; without them the gameplay spec skips explicitly and the smoke spec still runs (integration remains the mandatory DB gate). No live Google OAuth in CI. |
| **shadcn-svelte CLI init deferred** | `shadcn-svelte@1.5.0` is installed (devDep, as intended), but `init` requires an interactive design-system preset (no non-interactive preset name exists — `--preset new-york` rejected). Phase-1 UI is custom Wordle tiles/keys and needs no shadcn components; the CLI can be run later for Phase-2 onboarding UI without touching Phase-1 code. Non-blocking. |
| **`TS5112`/type-boundary board constants** | Board dimensions live client-side (`BOARD_ROWS`/`BOARD_COLS` in `src/lib/shared/lib/wordle-ux.ts`) rather than importing `$server/game/evaluate` at runtime into the bundle; a unit test pins parity with the server constants. |
| **Play page gate** | `src/routes/play/+page.server.ts` redirects unauthenticated SSR to `/`; Hono `requireAuth` remains the authoritative API gate (unit + E2E proven). |
| **smoke.spec.ts updated** | The scaffold heading (`Welcome to SvelteKit`) no longer exists; the smoke expectation now targets the real landing page. |
| **Preview env** | `vite preview` loads `.dev.vars` into the Worker bindings ("Using secrets defined in .dev.vars") — verified empirically; the E2E suite runs against preview, not wrangler dev. |
| **Finalization service shipped in Phase 1** | `finalizePuzzle` (src/server/puzzle/finalize.ts) is implemented because the NG9 lock-order tests must drive the REAL service (Phase-0 B6 mandate); the cron trigger wiring remains Phase 3. |
| **Client GAME_EXPIRED handling** | On a `409 GAME_EXPIRED` from a guess, the play page disables the board and shows the ended-puzzle message (local ephemeral state; server truth unchanged). |
| **CI job serialization (review finding)** | `e2e` now `needs: [unit-and-build, integration]` — both jobs TRUNCATE the app tables on the same non-production DB; parallel runs could wipe each other's fixtures mid-run. |
| **Word-list drift gate (review finding)** | CI runs `git diff --exit-code` on both generated word-list artifacts right after `bun run word-list` — a stale committed artifact now fails loudly. |
| **ALLOW_DB_WIPE trust level (accepted nit)** | The wipe opt-in is mechanism-level trust, not a URL heuristic: the dedicated non-production DB is designated by user decision (Phase-0 handoff §9). A production-looking-URL denylist was considered and rejected — false positives on the operator's real non-prod URL would break CI (its exact shape is not known to code). Defense-in-depth remains: flag required, secrets gitignored, CI injects them explicitly. |
| **403 vs 404 on foreign game (accepted nit)** | The 404-vs-403 difference for a valid uuid owned by another user reveals existence. Accepted: ids are unguessable `gen_random_uuid` values a caller can only know by owning them, and the Phase-1 spec mandates 403 FORBIDDEN for ownership failure. docs/phase-1-api.md documents the surface precisely. |

## Phase 2 planning resolutions (2026-08-25 — decided, not yet implemented)

Planning is complete (`phases/phase 2/phase-2-plan.md`); these decisions bind the
Phase-2 implementation chat unless the repository or a newer decision
supersedes them.

| Decision | Resolution |
|---|---|
| **Onboarding gating** | `isOnboarded(user)` = `onboarding_completed_at IS NOT NULL`. ANY authenticated user with incomplete onboarding is SSR-redirected from EVERY application route (`/play`, `/profile`, `/leaderboard`, `/admin`) to `/onboarding` — `/onboarding` is the only reachable application surface while incomplete; `/admin` additionally enforces the role guard after onboarding. Complete users are redirected from `/onboarding` to `/play`. Pre-Phase-2 accounts (existing friends) onboard once on next login — recorded deviation from Spec §1's "existing user → app" shortcut (spec assumed complete profiles; no data migration). |
| **Onboarding atomicity** | Atomic by single request: while incomplete, `PATCH /api/me/profile` requires BOTH `displayName` and `avatarEmoji` (else `INCOMPLETE_ONBOARDING` 400); nothing persists until the successful submit. Post-onboarding edits accept either field. |
| **Display name rules** | Charset `[a-z0-9 _-]` (case-insensitive) after trim + whitespace-collapse; canonical form 2–15 chars. `canonicalizeDisplayName` (uniqueness key → `display_name_normalized`, UNIQUE final guard) and `moderationKeyForDisplayName` (leet/confusable map + separator strip + banned-substring detection) stay SEPARATE functions. Generic `NAME_MODERATED` (400) message never reveals the offending word. New codes: `INVALID_NAME`, `NAME_MODERATED`, `NAME_TAKEN` (409), `INVALID_AVATAR`, `INCOMPLETE_ONBOARDING`. No name-change cooldown in V1. |
| **Reserved display names** | App-level set `['admin','wordle','leaderboard','moderator','system']`; rejected with the SAME `NAME_TAKEN` 409 as duplicates (UI cannot distinguish). |
| **Moderation baseline (user decision)** | Curated baseline authored directly in `src/lib/shared/config/banned-words.json` (provenance/version/license fields inside the JSON; ~60–100 entries); project overrides = edits to the same file. Same list + logic for onboarding and later profile changes. Third-party corpus import explicitly NOT chosen. |
| **Avatar** | Canonical curated `src/server/data/avatar-emojis.ts` (`{emoji,label}[]`, ~24, stable order) → generated client twin `src/lib/shared/config/avatar-emojis.generated.ts` via `scripts/build-avatar-list.ts` + `bun run avatar-list` + parity unit test + CI `git diff --exit-code` (mirrors the word-list pipeline). Server allow-list validation; DB stores the emoji string only; selection mandatory in onboarding; a11y labels, 48px+ targets, keyboard-native grid. |
| **Theme** | Binary `light | dark`; `localStorage['theme']`; system (`prefers-color-scheme`) is the default and is only overridden by an explicit choice; pre-paint inline script in `src/app.html` sets `data-theme` (NG17: keep single/scripts hashable for Phase-5 CSP); Tailwind v4 dark variant becomes `@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *))` replacing the current media-query blocks. No DB involvement. |
| **Shell/navigation (user decision)** | Tabs Play | Leaderboard | Profile (+ Admin for `role='admin'`) all render in Phase 2; Leaderboard (Phase 3) and Admin (Phase 4) are placeholder pages with real route guards (auth; admin role). Active-state styling + mobile fit in the header (no bottom nav in V1). Not-onboarded users see a header without tabs. Logout in header AND profile page (Spec §2). |
| **Admin bootstrap (NG18)** | Promote-only step in the Hono `authContext` middleware: when `ADMIN_EMAIL` binding is set, `email === ADMIN_EMAIL`, and `role <> 'admin'` → `UPDATE ... SET role='admin' WHERE id=$id AND role <> 'admin'` (idempotent; WHERE makes it a no-op afterwards) and refresh the context user. Never demotes; changed `ADMIN_EMAIL` never demotes; no-admin state = manual operator bootstrap. Known page-level lag: SvelteKit locals resolve independently, so the Admin tab appears on the next page load after promotion. |
| **TanStack scope** | New server state `['me']` (GET /api/me) + profile mutation (PATCH /api/me/profile) with cache update from the server response; NO optimistic mutations (same rationale as Phase-1 guesses). Theme + form input + picker selection stay local Svelte state. `@tanstack/svelte-form` added for the profile/onboarding form (architecture intent). |
| **shadcn-svelte** | CLI initialized in Phase 2 (interactive preset; non-TTY fallback = hand-rolled components with this deferral re-recorded). Use Input/Button/Badge (+Dropdown/Sheet only if genuinely useful) for profile/onboarding/shell; board/keyboard/tiles stay custom (documented non-goal). |
| **FSD/shared-module boundary** | `src/server` must not import FSD `src/lib`: display-name logic ships as a parity-tested twin (`src/server/profile/display-name.ts` authoritative + `src/lib/shared/lib/display-name.ts` client twin, parity unit test). If the implementer judges duplication worse than the boundary rule, the deviation MUST be recorded here before importing across. |
| **DB impact** | NONE — `avatarEmoji`, `role`, `display_name_normalized`, `onboarding_completed_at` already exist (generated schema + 0000_init). No migration; no Better Auth regeneration; `auth:check` stays green. |
| **Name ownership after onboarding (Google re-auth regression, v22)** | After onboarding, `user.name` + `display_name_normalized` are application-owned: ONLY `PATCH /api/me/profile` writes them. A later Google sign-in / session resolution must never rewrite them (the better-auth config maps no provider profile into user fields; `authContext`'s only user write is the admin role promotion). Pinned by an integration regression: complete onboarding → fresh session resolution → name unchanged. |
| **Moderation false-positive guard (v22)** | Substring moderation stays aggressive (evasions rejected) but MUST be validated against realistic benign names containing ordinary substrings (accepted); the curated dataset + rule are tested in both directions. Do not weaken detection to accommodate an edge case. **Dataset rule (v23):** the curated banned-word dataset must avoid entries so short/ambiguous that literal substring matching systematically produces false positives — the benign-name tests are the acceptance gate for the selected dataset (fix the dataset or the algorithm; never weaken the detector to pass the gate). |
| **Display-name parity rigor (v22)** | Server/client display-name twin parity is table-driven/property-based across ALL five behaviors (charset, whitespace normalization, canonicalization, moderation transformation, reserved names) — not a handful of examples. |
| **Avatar set reproducibility (v22)** | The exact committed emoji set (emoji + label + ordering) must be DOCUMENTED in the artifact and the decision log; tuning is allowed during implementation, an undocumented set is not. |
| **`types:check` blocker semantics (v22)** | `bun run types:check` runs in the hermetic clean-checkout condition (no `.env`/`.dev.vars`). A failure in that condition is a BLOCKER; a local failure must not be dismissed as "expected" without reproducing the clean-checkout condition and recording its result. |
| **Schema-purity proof (v22)** | Phase-2 completion includes `git diff --exit-code -- src/server/db/schema.ts src/server/db/migrations` (must be empty) — concrete proof of the no-schema-change decision. |
| **Library-guidance honesty (v22)** | If the `@tanstack/*` intent/skill tooling produces no usable documentation, agents must not imply the library skill was consulted; inspect the installed package's types/API and the official TanStack docs instead. |

## Phase 2 implementation resolutions (2026-08-26 — implemented, code-first)

Phase 2 (onboarding, profile, theme, application shell) is implemented and
verified. Every decision below is recorded because it either changes a
dependency, adds a config detail, fixes a Phase-1 latent bug, or exercises a
previously-recorded boundary/decision.

| Decision | Resolution |
|---|---|
| **Hono RPC chaining shape (schema threading)** | Registering Phase-2 `/api/me` routes through a second register function required a specific signature: `registerProfileRoutes<S extends Schema = {}>(app: Hono<AppEnv, S>, deps)` — the SCHEMA parameter must be a generic while the ENV stays the concrete `AppEnv`. A `T extends Hono<AppEnv>` generic erases the incoming game schema (method calls resolve against the constraint's empty schema), and a generic env makes zValidator's handler context conditional fail to reduce — both verified empirically with `hc` type probes. The Phase-1 chaining rule is preserved: the client still sees game + me/profile endpoints. |
| **`@tanstack/svelte-form` 1.33.5 + TanStack Form submit quirks** | Added (runtime dep) for the onboarding/profile forms. Two implementation findings: (1) `createMutation` (svelte-query v6) `isPending` can stay `true` after a resolved `mutateAsync` — the form's own `isSubmitting` is the submit-state owner instead (comment in the pages). (2) `form.handleSubmit` does NOT prevent the native form submission when client validation blocks `onSubmit` — the pages wrap it: `onsubmit={(e) => { e.preventDefault(); form.handleSubmit(); }}` (E2E scenario 4 caught the native `?displayName=x` reload). |
| **shadcn-svelte 1.5.0 initialized — vega preset, non-interactive via PTY** | `init` has no non-interactive preset name (`--preset new-york|vega` both rejected as "not a valid preset"); the interactive flow was driven through a PTY accepting the defaults (preset **vega** — Lucide/Inter classic, base color neutral). `components.json` written; `src/lib/components/ui/{button,input,badge}` added; `button.svelte` carries a scoped eslint-disable for `svelte/no-navigation-without-resolve` (generated link variant; callers pass resolved strings). shadcn dark tokens in `src/app.css` re-pointed from the generated `.dark` class to `:root[data-theme='dark']` so persistence drives them. Deps added (exact resolved): `@fontsource-variable/inter@5.3.0`, `clsx@2.1.1`, `tailwind-merge@3.6.0`, `tailwind-variants@3.3.1`, `tw-animate-css@1.4.0`, `@tanstack/svelte-form@1.33.5` (+ `@tanstack/form-core@1.33.5`, `@tanstack/svelte-store@^0.12.0` transitively). shadcn components land in `src/lib/components/` (the CLI default alias, `$lib/components`) — documented FSD accommodation for generated third-party primitives. |
| **Banned-word dataset final state (v23 gate applied)** | 97 entries; the benign-name acceptance gate (66 realistic handles incl. class/glass/massive/document/title/visit/spice/mickey/bobby/bob/adam/peacock/bob/analog/analyst/washington/… — `peacock` forced the exclusion of `cock`, which also collides with cocktail/cockpit/cockerel) and the evasion gate (f u c k / f4ck / fuuck / fack / sh1thead / a55hole / bullsh1t / n1gger / …) both pass. `fack` added as an embedded evasion variant (f4ck's key); `ashole` was briefly considered and dropped (redundant — `asshole`'s key IS `ashole`). Key semantics pinned by tests: the moderation key applies the leet map, strips separators `[-_. ]`, and collapses DUPLICATE RUNS only — `fuck`→`fuck`, `fuuck`→`fuck`, `bullshit`→`bulsit`, `asshole`→`ashole`. |
| **FSD boundary — banned-words JSON imported as inert data** | Decision-log v22 allowed a recorded deviation: `src/server/profile/display-name.ts` imports `../../lib/shared/config/banned-words.json` (the D3-mandated canonical path). Duplicating the curated dataset server-side would violate D3's one-list rule; it is inert data with provenance; the twin tests pin both modules against the SAME file (identity is structural). |
| **Admin bootstrap env guard (bug found by unit tests)** | `applyAdminBootstrap` dereferenced `env.ADMIN_EMAIL` — `c.env` is `undefined` in Hono's `app.request()` test path (routes.ts guards the same case for `/api/auth/*`), so EVERY authenticated API request under test 500'd. Fixed: `env` accepted as possibly-undefined; no env ⇒ no bootstrap. |
| **Phase-1 latent bug: header sign-out 403 INVALID_ORIGIN in preview/E2E** | Better Auth's origin check rejects state-changing requests whose Origin ≠ baseURL origin; `BETTER_AUTH_URL` is `http://localhost:5173` while preview/E2E run at `127.0.0.1:4173`, so `POST /api/auth/sign-out` returned 403 and logout silently failed (no Phase-1 E2E covered logout). Fixed in `src/server/auth/auth.ts`: `trustedOrigins: ['http://localhost:5173', 'http://127.0.0.1:4173']` (production origins come from `BETTER_AUTH_URL` itself; CSRF stays fail-closed). E2E scenario 12 pins it. |
| **Theme + FOUC** | Pre-paint inline script in `src/app.html` (single, NG17-hashable) sets `data-theme` before first paint; `@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *))` replaces the Phase-1 media-query dark behavior; `color-scheme` follows the attribute. E2E scenario 11 + the capture audit verified `data-theme` and persistence across reload in light+dark at both viewports. |
| **Avatar set (D4 reproducibility)** | 24 curated entries, stable order, no skin-tone sequences: Fox, Wolf, Panda, Frog, Owl, Koala, Lion, Tiger, Pig, Monkey, Octopus, Unicorn, Dragon, T-Rex, Turtle, Shark, Dolphin, Bee, Butterfly, Ladybug, Crab, Penguin, Seal, Hamster — documented in the artifact header + this table (the exact committed set). |
| **E2E fixture parity** | `createAuthenticatedUser` now canonicalizes the inserted `name` (production stores canonical names) and supports `onboarded` / `role` / `fresh:false` (coexisting users for the duplicate-name scenario). Phase-1 gameplay specs run as onboarded users (unfinished onboarding redirects all application routes). |
| **Shell state + header data flow** | Header renders from SSR locals while `['me']` is pending, then from the TanStack cache (name/avatar/role updates reflect immediately after saves/promotion; documented page-level lag for the Admin tab stays). Desktop header is ONE row (max-w-3xl; brand | tabs | theme/chip/logout); mobile keeps TWO rows (brand+avatar-only chip+controls, then full-width tabs). Verified by DOM geometry audits at 1440×900 and 390×844. |
| **Visual QA environment honesty** | This implementation environment has NO vision input (no image tool available), so screenshots at `.cache/ui-shots/` (20 files: 5 surfaces × desktop/mobile × light/dark) were captured but could NOT be opened and inspected. In lieu of inspection: automated per-viewport audits (horizontal overflow, WCAG 2.5.8 target sizes ≥24px with 48px avatar targets, accessible names, focus indicators, computed-style contrast ≥4.5 for body/input/submit text, pre-paint `data-theme` correctness) — all green after the header layout fixes. Checkpoints A/B/C are therefore **structurally verified, not visually verified**; a human with vision should open `.cache/ui-shots/`. |
| **Screenshots and scratch tooling** | `.cache/ui-shots/` + the QA harness scripts stay gitignored (not committed). |
| **Admin bootstrap email comparison (v25 review finding, fixed)** | `applyAdminBootstrap` compared `auth.user.email === adminEmail` case-sensitively. Better Auth stores the provider email VERBATIM (callback flow persists `userInfo.email` as received), and Google Workspace identities may be mixed-case — a mixed-case `ADMIN_EMAIL` binding would silently never promote (bootstrap is the only in-app admin path; recovery is manual SQL). Fixed: trimmed + lowercased comparison on BOTH sides (consistent with better-auth's own account-linking email comparison), keyed on user id; never demotes. Pinned by integration bootstrap cases 6–7 (mixed-case + padded `ADMIN_EMAIL` promotes; non-matching stays player). |

## Phase 3 planning resolutions (2026-08-27 — decided, not yet implemented)

A read-only Phase-3 planning pass (see `docs/phases/phase 3/phase-3-plan.md` and
`phase-3-planning-state-handoff.md`) resolved every technical Phase-3 question against the
actual repository at `1d91865`. These decisions bind the Phase-3 implementation chat unless
the repository or a newer decision supersedes them. Previously-open items NG11–NG14 and M1
are now concretely specified by these resolutions.

| Decision | Resolution |
|---|---|
| **No schema migration** | All Phase-3 columns already exist in `0000_init` (`daily_puzzles.average_completion_time_ms`/`non_completion_penalty_ms`/`finalized_at`, `games.completed_at`/`completion_time_ms`/`guess_count`, `user.name`/`avatar_emoji`). Schema-purity diff gate stays mandatory: `git diff --exit-code -- src/server/db/schema.ts src/server/db/migrations` (EMPTY). |
| **No ranking table** | Aggregation = `DENSE_RANK()` window queries over raw facts; `user_id` only in the final display `ORDER BY`, never in the rank window (NG14/M2). Dense ranks; `?limit=` is a dense-rank cutoff (`rank <= limit`, default 10, cap 50), not a row cap (NG11). |
| **MISSED derivation (NC1, closed)** | MISSED = LEFT-JOIN absence of a `games` row per `(user_id, puzzle_id)` on finalized eligible days, COALESCE'd to the frozen `non_completion_penalty_ms` + 6 guesses. No fake rows, no placeholder rows, raw model untouched. FAILED/FORFEITED use the same frozen penalty; single-day boards exclude all non-completed results (Spec §11). |
| **Day-set semantics** | Today/Yesterday: COMPLETED only. Week (ISO Monday, `WEEK_START = MONDAY`, M1): finalized days with non-NULL averages + today completed-only. Month: `date_trunc('month')` through today, same rule. Zero-completion finalized days contribute nothing (excluded structurally at the frame level). Today's FAILED/FORFEITED/MISSED are ignored until finalization — no denominator slot. Boundaries computed in SQL from `transaction_timestamp() AT TIME ZONE 'Asia/Manila'`. |
| **Tiebreaker day set (NG12, closed)** | `earliest_qualifying_completion_at` = `MIN(completed_at)` over "the same day set used for the score average" (spec-literal), so today's completion participates for players whose average includes today. |
| **Qualification** | `QUALIFIED ⇔ completedDays >= threshold`; `completedDays` counts COMPLETED games on finalized eligible days only (today excluded). Constants live in `src/server/leaderboard/constants.ts` (`WEEKLY_QUALIFICATION_COMPLETED_DAYS`, `MONTHLY_QUALIFICATION_COMPLETED_DAYS`) with **PROVISIONAL values** — exact values remain an open product decision (spec §23); the file asserts `>= 1`. API exposes `currentUser.qualified` + `completedDays`. |
| **Settlement execution (NG2-er-adjacent cron wiring finding)** | `@sveltejs/adapter-cloudflare@7.2.9` has NO entrypoint option and its fixed worker template exports only `fetch` (verified in installed package). Resolution: deterministic post-build patch — `scripts/patch-worker-scheduled.ts` esbuild-bundles `src/server/puzzle/scheduled-entry.ts` → `.svelte-kit/cloudflare/_settlement.js` and appends `import { scheduled } from "./_settlement.js"; export { scheduled };` to `_worker.js`; a vite `closeBundle` hook runs it on every `bun run build`. Domain logic stays in `src/server/puzzle/settlement.ts` (`finalizeExpired` FOR UPDATE SKIP LOCKED + existing idempotent `finalizePuzzle`; `activateToday`; `runSettlement`). No second API/server architecture; no SvelteKit-route domain logic. |
| **Lazy finalization** | Read-path reconciliation: `finalizeExpired()` runs (own transactions) before Week/Month aggregation; today/yesterday never need it. The tiny unreconciled window (expired ACTIVE historical day mid-sweep) excludes that day for one read — bounded, self-healing; recorded behavior. |
| **Concurrency preserved** | Puzzle-row lock first, `transaction_timestamp()` anchor, no `clock_timestamp()` (NG9). Sweep uses `FOR UPDATE SKIP LOCKED`; per-puzzle idempotency covers overlap. Existing midnight lock-order A/B tests unchanged. |
| **API surface** | 4 GETs `/api/leaderboard/{today,yesterday,week,month}` (Architecture shape verbatim), `?limit=` dense cutoff, `currentUser: { rank, qualified, completedDays, entry }` (NG13 extended with qualification state), `requireAuth` on `/api/leaderboard/*`, onboarding NOT enforced at API level, NG21 envelope. NO `/api/game/history`, NO `/api/stats`, NO manual settlement/activation endpoint, NO `/api/leaderboard/me`. |
| **History/statistics scope** | Per Spec §22 ("needed to support the leaderboard") Phase 3 adds no dedicated history/statistics surface. The multi-day aggregations ARE the statistics; the result screen reuses game state + the today board. |
| **Result screen** | COMPLETED → `Current position: #N` (dense rank from `['leaderboard','today']`) + explicit "position may change" caption + "View leaderboard" (Today default tab); FAILED/FORFEITED → penalty-applies notice, no position; position block hides silently on fetch failure. |
| **UI placement** | Leaderboard rows + position callout extracted to `src/lib/features/leaderboard/` (real reuse across `/play` and `/leaderboard`; FSD extraction rule satisfied; fallback = shared/ui + recorded deviation). shadcn `Tabs` for periods; TanStack keys `['leaderboard', period]`; current-user highlight via `['me']`. |
| **Missing-puzzle alerting** | Fail-closed already implemented (`PUZZLE_UNAVAILABLE`); cron missing-today adds a structured `console.error` marker (`[settlement] missing puzzle for date=...`). Notification/email binding deliberately deferred (no invented infrastructure; Phase-6 decision). |
| **Indexes** | No new indexes — existing `daily_puzzles(puzzle_date)` unique, `games(puzzle_id, status)`, `games(user_id, puzzle_id)` unique cover all plan-§10.4 query shapes for a small private group. |
| **CI** | Extend existing three jobs in place: `unit-and-build` gains the schema-purity diff step + a patched-worker assertion (`grep -q "export { scheduled }" .svelte-kit/cloudflare/_worker.js`). New unit/integration/e2e suites run in the existing jobs; `wrangler dev --test-scheduled` documented locally, not CI. |
| **Open product decisions (NOT resolved by planning)** | P1 threshold values (provisional in constants); P2 penalty for pre-join days (plan default: spec-literal uniform; carve-out needs a product decision); P3 alert channel (log-only now); P4 avg-guesses display precision (2dp display-only); P5 `?limit=` cap (50); P6 result-block copy (meaning fixed). |

### Phase 3 implementation deviations (2026-08-30 — recorded per the implementation prompt §3 rule)

| Deviation | What was done and why |
|---|---|
| **Calendar-adaptive integration fixtures** | The day frames are anchored to the real DB clock (`transaction_timestamp()` Manila), so a test cannot fabricate "today". On frame-poor calendar days (e.g. Mon–Wed in a week, or the 1st–7th of a month) the PROVISIONAL thresholds (3/8) cannot be reached by ANY fixture — the week/month boards are then necessarily empty. The suites compute frame capability in SQL and assert the deepest semantics the calendar allows, ALWAYS asserting the scored-level facts (completedDays, the documented empty-board rule) — never a silent skip. Full aggregation math (frozen MISSED penalty inside a qualified player's average, ranking determinism, tiebreaker day set) asserts on the ~25+ days/month where the frame can host the threshold. |
| **Tiebreaker-day-set test uses constructed raw timestamps (I13)** | `completed_at` is a raw game fact; the fixture may set any value. In REAL wall-clock terms a today-dated completion is always later than past-day completions, so "today's participation" would never change `MIN(completed_at)` — the domain rule (the MIN covers the same day set as the average, D5) is therefore validated with constructed instants (today-dated game @09:00Z vs past-day completions @10:00Z/@12:00Z). Semantics unchanged; only the test data is synthetic. |
| **Worker entry typed with local structural platform types** | `scheduled-entry.ts` declares `ScheduledController`/`ExecutionContext`/handler shape structurally instead of importing `cloudflare:workers` — this project's TS program deliberately does NOT load `@cloudflare/workers-types` (hermetic `types:check`; DOM-lib conflicts), so the import would not resolve. Runtime behavior is identical; the plan's type name (`ExportedHandlerScheduledHandler<HonoBindings>`) is reproduced as a local alias. |
| **E2E runner serialized (`workers: 1`)** | Phase-3 added a second fixture-using spec file; the previous `fullyParallel: true` let multiple workers TRUNCATE the shared non-production DB concurrently (observed mid-test state wipe). The runner is now explicit: one worker, serial tests. No test was weakened — this removes a genuine race. |
| **U5 answer-material assertion form** | The chunk scan asserts absence of the game/data layer (module identifiers would appear verbatim in non-minified esbuild ESM output) + the private answer pool when seed files are present. Bare word scanning is impossible: library error templates legitimately contain substrings (e.g. `"information about operator class"`, pg `"drain"` event names). |
| **`types:check` hermetic condition confirmed** | `wrangler types` picks up `.env`/`.dev.vars` values if present locally, so the parity gate must run with those two gitignored files absent (the committed `worker-configuration.d.ts` is generated without them; the phase-2 handoff already labeled the gate "hermetic clean-checkout condition"). The patched `_worker.js` does not change the generated types (verified). |
| **E5 rank pinning** | The result-position spec pins the viewer's dense rank with a 200ms rival + deliberate typing delays (the real play's elapsed is bounded to (200ms, 10m)) after observing a sub-second synthetic play beat a 3s rival; the exact rank number is display behavior, the block's presence/copy is the asserted contract. |
| **Client never sends `?limit=`** | The wire supports the dense cutoff, but the UI uses the server default (10) everywhere; the typed client keeps the parameter free for future use. No semantics change. |
| **CI failure #1 (fixed 2026-08-30, `929ec44`)** | The first GitHub Actions `unit-and-build` run failed in `bun run build`: vite 8.2.2 fires `closeBundle` once per build environment, and the CLIENT phase runs before `@sveltejs/adapter-cloudflare` writes `.svelte-kit/cloudflare/_worker.js`. On a fresh checkout the hook's `patchWorker` threw ("worker output not present"), the hook set `process.exitCode = 1`, and the build exited 1 despite the final phase patching correctly (a stale local `.svelte-kit` had masked the race). Fixed: `patchWorker` now DEFERS (`skipped`/`reason: 'deferred'`) when the worker output is missing — the final (server) build phase is the authoritative patch point and the CI patched-worker assertion guards a silent miss; direct operator runs keep a loud `failIfMissing`. A U5 regression test pins both behaviors. Also added the `.ts` extension to the `vite.config.ts` import (clears vite's native-configLoader warning). Re-verified on the exact failing condition (fresh checkout build: client phase defers, final phase patches, exit 0). |
| **CI failure #2 (fixed 2026-08-30, I5c rewrite)** | The `integration` job failed on I5c ("expected 0 to be greater than or equal to 1"): the test asserted at least one finalization from two concurrent sweeps racing an expired-puzzle guess. That interleaving is genuinely nondeterministic — the guess holds the puzzle row lock (`FOR UPDATE`) during its short rejection round-trip, and `FOR UPDATE SKIP LOCKED` then makes BOTH sweeps legitimately skip the row (skip-locked by design: the guess won the serialization point). The product guarantees are different and now asserted deterministically: (1) a sentinel-held lock pins that a sweep skips locked rows (returns `[]`); (2) the guess can never succeed and writes nothing (NG9 discipline); (3) at most ONE real finalization across two concurrent sweeps (finalizePuzzle serializes; SKIP LOCKED + idempotency cover the rest); (4) a raced-away sweep never loses the day — the NEXT sweep finalizes the row (self-healing reconciliation, the plan's documented lazy/missed-cron recovery). I5c rewritten and re-run 6× against live Neon + the full 55-test integration suite. |
| **CI failure #3 (fixed 2026-08-30, batched month fixtures + timeout headroom)** | The `integration` job timed I9 out at 30s. Root cause: I9/I11/I12 seed `dayOfMonth − 1` (up to 30) finalized days, each via its own answer + puzzle insert + per-day game inserts — ~60+ Neon round trips whose cost scales with the calendar day. Locally (runner near the Neon region) I9 took ~27s, already scraping the cap; the CI runner (eastus2 ↔ ap-southeast Neon) pushed it past 30s. Fixed structurally: `seedFinalizedDays` and `insertGamesBatched` now issue ONE parameterized multi-row INSERT per table (`sql.join` — same shapes, raw model untouched), and date lists come from a single `generate_series` query. I9 dropped from ~27s to <2s; the full suite went 314s → 58s. Also raised `vitest.config.ts` `testTimeout` 30s → 60s (documented headroom for cross-region latency, not a mask — the sequential suites legitimately run long; `fileParallelism: false` unchanged). |
| **Audit A — sweep `FOR UPDATE SKIP LOCKED` scope (resolved 2026-08-30, no mechanism change)** | The Phase-3 correctness audit flagged that `finalizeExpired()`'s `SELECT … FOR UPDATE SKIP LOCKED` runs through `db.execute()` in autocommit (the statement's row lock ends with the SELECT), while the real finalization happens per row in `finalizePuzzle()`'s own transaction re-acquiring the puzzle lock. Evaluation result: the mechanism exactly matches the plan's authoritative contract (plan §6: sweep-level SKIP LOCKED selection + per-row `finalizePuzzle` "idempotent, own transaction" whose already-FINALIZED re-entry "covers the rest"). Correctness holds in EVERY interleaving — two concurrent sweeps may both select a row, but finalizePuzzle re-locks puzzle-first (NG9) and at most ONE real finalization happens (losers observe FINALIZED via READ COMMITTED post-lock re-read → write-free `alreadyFinalized`); rows skipped because another transaction held their lock self-heal on the next sweep/lazy read. Holding the sweep's SELECT locks across finalization was evaluated and REJECTED: per-row own-transaction finalizePuzzle with a pooled driver runs on a different connection than the held lock → deadlock (the plan's structure is the correct one). Action taken: the code comments now state the precise semantics (SKIP LOCKED = soft selection filter; idempotent finalizePuzzle = the authoritative guard), and a dedicated integration test **I5d** pins the guarantee directly (two concurrent sweeps → ≤1 real finalization, exactly one frozen average/penalty set, FORFEITED conversion, raw facts untouched; run 5× on live Neon). |
| **Audit B — swallowed cron failures (resolved 2026-08-30, changed)** | The audit flagged that `scheduled()` caught + logged `runSettlement()` failures without rethrowing, so a failed settlement looked successful to the platform (invocation resolves OK). Resolution: the handler now rethrows after the structured log, so the cron invocation is marked FAILED in the Cloudflare dashboard. This is a recorded deviation from the plan §7.3 literal wording ("errors are caught and logged") — the plan's own parenthetical ("a thrown error still marks the run failed in the dashboard") is the operative intent, and the repo's fail-closed + structured-marker philosophy favors surfacing. Rethrowing adds no risk: cron deliveries are at-most-once (no auto-retry duplication) and `runSettlement` is retry-safe by construction (SKIP LOCKED selection + idempotent finalizePuzzle/activateToday + self-healing lazy reconciliation), so a re-invocation or the next run is always harmless. Pinned by a DB-free unit test (missing `DATABASE_URL` env → structured `[settlement] run failed` log + rejects). |

## Phase 4 decisions (2026-08-30 — recorded FIRST, before any Phase-4 implementation)

Binding decisions D1–D10 from `docs/phases/phase 4/phase-4-plan.md` §5, recorded per the
repo decision-log rule (they are the plan authors' choices; the implementation chat
executes them as-is).

| # | Decision |
|---|---|
| **D1** | **API role gate**: `requireAdmin` middleware (403 `FORBIDDEN` envelope when the authenticated user's `role !== 'admin'`) composed as `.use('/api/admin/*', requireAuth)` then `.use('/api/admin/*', requireAdmin)` in `routes.ts`. The PAGE guard (`requireAdmin` in `src/lib/app/guards.ts`) stays as-is; page and API check independently. |
| **D2** | **FSD placement**: the admin UI is page-owned (single consumer). Components live under `src/routes/admin/` (e.g. `puzzle-calendar.svelte`); no speculative `$lib/features/admin/` slice. Only genuinely reusable pure helpers (e.g. date formatting) go to `src/lib/shared/lib/`. |
| **D3** | **Hint contract**: scheduling/replacement input carries `hintLetter`; server validation: `^[A-Z]$` (normalized to uppercase) AND the letter must occur in the answer word; persisted at scheduling time, never at activation (NG2). UI pre-fills the answer's first letter as the default; the admin may change it. |
| **D4** | **List window**: `GET /api/admin/puzzles?from&to` — YYYY-MM-DD, `from ≤ to`, window ≤ 120 days; defaults `from = today − 30 days`, `to = today + 90 days` (Asia/Manila, computed in SQL). Response ordered by `puzzle_date`. |
| **D5** | **Validation UX**: scheduling validation feedback ("Approved answer" / "Already scheduled/used" / "Not in approved answer list") is computed SERVER-side via `POST /api/admin/puzzles/validate`. The client never receives the answer pool. Invalid/duplicate answers are flagged by the calendar row + the form; submission is rejected server-side regardless of client checks. |
| **D6** | **Delete/edit state guards**: DELETE and PATCH — allowed ONLY when `locked_at IS NULL AND status = 'SCHEDULED' AND puzzle_date > today (Manila)`; any violation → 403 `FORBIDDEN` with a specific code (`PUZZLE_IMMUTABLE` for locked/ACTIVE/FINALIZED, `NOT_SCHEDULED` for non-SCHEDULED, `NOT_FUTURE` for non-future). Today's SCHEDULED puzzle: no plain delete/edit; PATCH with a today's date on today's SCHEDULED puzzle is NOT the replacement path. |
| **D7** | **Gap alerting**: move/delete operations that create a date gap log a structured marker (`[admin] puzzle gap created dates=…`) via the existing logger seam and return the gap dates in the response body; the UI warns + shows the affected dates. No new notification infrastructure (P3 defers real alerting to Phase 6). The settlement cron remains the operational detector for a missing TODAY. |
| **D8** | **Same-day replacement endpoint**: `POST /api/admin/puzzles/:id/replace-today { word, hintLetter }` — the ONLY way to change today's puzzle. Guards (inside the transaction, under the puzzle-row lock): `puzzle_date` = today (Manila), `status = 'SCHEDULED'`, `locked_at IS NULL`; then UPDATE `answer_id`+`hint_letter`+`expires_at` (recomputed via `expiresAtExpr`). Never delete+reschedule. |
| **D9** | **Date moves**: PATCH `puzzleDate` on a future SCHEDULED puzzle recomputes `expires_at`, re-checks `UNIQUE(puzzle_date)` (pre-check + 23505 → 409 `DATE_TAKEN`), destination must be > today. |
| **D10** | **Error codes**: add to `ERROR_CODES`: `ANSWER_NOT_APPROVED` (400), `INVALID_HINT` (400), `ANSWER_ALREADY_SCHEDULED` (409), `DATE_TAKEN` (409), `PUZZLE_IMMUTABLE` (403), `NOT_SCHEDULED` (403), `NOT_FUTURE` (403), `INVALID_DATE_WINDOW` (400). All responses use the NG21 envelope. |

Also recorded (plan §2.4 — product-tunable / deferred, NOT implemented): D4's window
defaults are product-tunable (parameterized API; changing the UI default later does not
re-plan this phase); the **admin settlement tooling** exclusion is a scope decision
(Phase-3 handoff "NOT promises" — adding it requires a product decision); P1–P6 stay
PROVISIONAL/untouched.

### Phase 4 implementation deviations (2026-08-30 — recorded FIRST, before the deviating code)

| Deviation | What was done and why |
|---|---|
| **`expiresAtExpr` corrected inside `src/server/puzzle/manila.ts` (off-by-8h bug)** | The plan said to reuse `expiresAtExpr(puzzleDate)` verbatim. Probing PostgreSQL semantics showed the expression `((${puzzleDate}::date + 1) AT TIME ZONE 'Asia/Manila')` resolves to a `timestamp WITHOUT time zone` (the date value `2026-09-05` is first coerced to `timestamp` and `date AT TIME ZONE zone` then produces the GMT-midnight WALL time `2026-09-05 08:00:00` as a naive timestamp). Inserted into the `expires_at TIMESTAMPTZ` column, PostgreSQL interprets that naive value in the SESSION timezone (GMT on Neon) → the stored instant is `2026-09-05T00:00:00Z`, exactly 8 hours LATER than the NG1 contract `(puzzle_date + 1) AT TIME ZONE 'Asia/Manila'` (Manila midnight = `16:00:00Z`) — and it would shift with any non-UTC session timezone. The helper was previously unused (Phase 0–3 fixtures store `new Date(...)` directly), so Phase 4 is its first consumer and the plan's own acceptance criteria (I-A1/I-A4/I-A6: `expires_at` = SQL-computed `(date + 1) AT TIME ZONE 'Asia/Manila'`) cannot pass otherwise. Fixed to `((${puzzleDate}::date + 1)::timestamp AT TIME ZONE 'Asia/Manila')` — the explicit `::timestamp` cast selects the `timestamp AT TIME ZONE zone → timestamptz` operator, verified live against Neon (`2026-09-04 16:00:00+00`, `pg_typeof = timestamp with time zone`). Semantics: unchanged (still NG1); only the expression's type resolution was corrected. |
| **`verify:bundle` whole-build scan vs the `answers ⊂ valid guesses` subset rule (fixed 2026-08-30)** | The plan requires BOTH (a) the pool ⊆ public valid-guesses subset rule (NG13, enforced at import) and (b) `bun run verify:bundle` green with a populated local seed file. These collide: the client bundle contains the PUBLIC guesses list by design (the keyboard validates against `src/lib/shared/data/valid-guesses.json`; empirically `flame`/`about`/`tiger` appear in the /play client node chunk AND the SSR page chunk), so EVERY compliant pool word is found by the scan's raw substring check — the gate can never pass with any populated pool. The scan's real value is flagging words that are NOT in the public list (a non-compliant/private word would be a genuine leak). Fixed in `scripts/verify-bundle-secrecy.ts`: pool words that are members of the public guesses artifact are documented as expected in the bundle and skipped; pool words NOT in the public list are still hard-scanned (any hit → exit 1). Verified: with a 2-word fixture (`rabbit` — non-public; `flame` — public) the gate now reports the non-public word absent and passes; the settlement-chunk answer-free pin (U5) and the new admin-secrecy unit pins remain the word-level leak checks, and the role gate (E-A1) the response-level one. |

## Phase 3+4 post-implementation visual-review decisions (2026-08-30 — UI-only, recorded with the fix commit)

Produced by the multimodal `make-ui-not-ai` review of the Phase 3/4 surfaces
(`docs/phases/phase 3 + 4 vision/phase-3-and-4-visual-review-final.md`; prompt in
`phase-3-and-4-multimodal-prompt.md`). UI-only — no domain, API, schema, or secrecy
changes; the fix commit is `1de4c02`.

| # | Decision / deviation | Record |
|---|---|---|
| **V-UI-1** | **Word-only calendar cells + state colors.** Cell content reduced to day number + word (+ lock icon); status is encoded by cell tint (faint neutral = Finalized, green tint = future Scheduled, solid green = Live today, amber tint = today-SCHEDULED) with a text legend under the grid, so color is never the only indicator; every cell is a `<button>` with a descriptive accessible name. **Deviation from plan §9's cell composition** (word + hint + badges + inline edit/delete affordances) which overflowed at 390px. |
| **V-UI-2** | **Click-to-view day-detail modal** (`src/routes/admin/day-detail.svelte`, page-owned per D2): formatted date, state text, word, hint letter, lock note, and state-appropriate actions (Edit + Delete / Replace today's puzzle / Schedule puzzle / view-only). Replaces inline cell actions. Empty future cells open the schedule form directly. |
| **V-UI-3** | **Full-month mobile grid.** All seven columns fit 390px (cell ≈43px, `truncate` backstop; wider words elide — full word one tap away in the modal). Replaces the intermediate horizontally-swipeable strip, which cut Fri–Sun off-screen with no affordance. |
| **V-UI-4** | **Leaderboard column headers** (`PLAYER / TIME · GUESSES`, week/month: `AVG TIME · AVG GUESSES · DAYS`). Deliberately a visual hint row — NOT an ARIA `row`/`columnheader` — so table row semantics and the E3 row-count assertions are unaffected (values keep their own aria-labels). |
| **V-UI-5** | **One accent system for primary actions.** New `green` Button variant (`tile-green` + white, both themes) used by admin schedule/edit/replace submits and the replacement-panel button; play/leaderboard CTAs already used it. Replaces black/white (`bg-primary`) primary in admin dialogs, which read as a second product. |
| **V-UI-6** | **Warning/destructive semantics.** Gap banner uses amber (warning) treatment like the cron-missed panel; the delete-confirmation button is a solid destructive (verified 4.77:1 light / 6.48:1 dark) instead of the pale tinted variant. |
| **V-UI-7** | **Readability fixes.** FAILED/FORFEITED penalty line `text-sm /65` (≈7.0:1 light / ≈8.3:1 dark); month callout caption is period-aware ("as the period progresses") via the existing `positionBlockCopy`; admin page title scale aligned to `text-xl`. |
| **V-UI-8** | **Name-length countermeasures verified.** Names are hard-capped 2–15 chars server-side (unchanged, unit-tested); row truncation proven with a 28-char stress name (desktop full render, mobile ellipsis, no layout break). No validation change. |

E2E (admin.spec.ts E-A2/E-A3/E-A4) was updated to the click-cell → modal interaction;
no existing assertion was weakened. Visual findings, the 21 reviewed screenshots, and
the final READY verdict are recorded in the visual-review receipt.

## Phase 5 resolutions (2026-09-02 — decisions recorded BEFORE code, Phase-4 discipline)

Decisions and verification results for the Phase-5 security hardening pass
(plan: `docs/phases/phase 5/phase-5-plan.md`). Every row below was decided or
verified during the S0 audit; the code that implements the plan-mandated
remediations follows these records.

| # | Decision / verification result | Resolution |
|---|---|---|
| **D2/F6 — RLS decision record** | Row-level security on `answer_dictionary`/`daily_puzzles` is **NOT implemented** (no schema change; plan §O.3 zero-migration invariant). Compensating controls verified in place: server-only DB access path (bridge passes no client role), `verify:bundle` post-build scan, admin-role exposure gate, answer pool gitignored. Defer to an explicit product+operator decision (plan §N D2); a schema change would require a new migration + a new decision. |
| **D9/F7 — `cookieCache` assessment** | Better Auth 1.7.1 `session.cookieCache` is **opt-in** (`session?.cookieCache?.enabled` — verified in `node_modules/better-auth/dist/cookies/index.mjs:77`) and **NOT adopted**: default off; the 5-minute staleness window would delay session revocation and sign-out invalidation, which the app pins today (e2e scenario 12). Assessment recorded: adoption only if performance data demands it, with revocation-semantics trade-off reviewed (plan §N D9 — default NOT adopted). |
| **Page `X-Request-Id` NOT added** | Plan §H: pages do not get `X-Request-Id` (API-only; correlation via access logs, avoiding duplicate header semantics). Page surfaces carry nosniff / XFO DENY / Referrer-Policy / HSTS-on-https only. |
| **Hooks `/api/*` header skip (verified no duplication/conflict)** | The SvelteKit hooks wrapper applies the page baseline ONLY to non-`/api` responses. The bridge (`src/routes/api/[...path]/+server.ts`) passes the Hono response through hooks; Hono already emits the identical contract via `securityHeadersMiddleware` (`routes.ts:97`), so a second set() would be a duplicate owner. Verified at implementation: `/api/*` responses keep exactly the Hono header set. |
| **HSTS https-only on pages** | Pages gate HSTS exactly like the API `hstsOnHttps` middleware (`url.protocol === 'https:'`); the local `curl`/preview probe (http) therefore shows nosniff/XFO/Referrer but NOT HSTS — HSTS presence is unit-tested via a fake `https:` URL (plan G10 nuance, §H "over HTTPS [NEW S0]"). |
| **S0 verification task — raw SQL** | `grep -rn 'sql\`' src/server` → **NONE**. All queries parameterized (Drizzle); no string interpolation of user input. |
| **S0 verification task — handler audit** | All mutation bodies are zod `.strict()` (admin: 4 schemas; game: 1; profile: 1) — unknown fields rejected 400. `UUID_RE` short-circuit present in game + admin handlers (404 without DB round-trip). GET registrations are read-only by construction: `/api/game/current`, `/api/me`, `/api/leaderboard/{today,yesterday,week,month}`, `GET /api/admin/puzzles` (list). |
| **S0 verification task — GET-reachability (F8)** | Better Auth 1.7.1 registers `sign-out` with `method: "POST"` only (verified `node_modules/better-auth/dist/api/routes/sign-out.mjs`); `GET`/`HEAD` probes → 404 (no session invalidation possible). Registered mutation routes probe → 401 (guard) / 404 (no GET handler). Pinned by the new route-inventory unit test (`tests/unit/security-baseline.test.ts`). |
| **S0 verification task — `RATE_LIMITED` consumers** | `grep -rn 'RATE_LIMITED' src/` → only the definition at `src/server/lib/errors.ts:25`; zero consumers pre-S1 (S1 adds the 429 envelope path). |
| **S0 verification task — `{@html}`/`innerHTML`** | `grep` across `src/` → **no `{@html}` and no `innerHTML`** anywhere. No raw-HTML sinks (Svelte auto-escaping is the only rendering path). |

### S1 rate-limiting decisions (2026-09-02 — verified against installed toolchain, recorded before code)

| # | Decision | Resolution |
|---|---|---|
| **S1a — binding schema (plan [NV])** | Installed wrangler 4.125 validates `[[rate.limit]]` bindings with ONLY: `name` (string), `namespace_id` (string), `simple: { limit: number, period: number }` where `period` MUST be 10 or 60 (verified `validateRateLimitBinding` in `node_modules/wrangler/wrangler-dist/cli.js:55419`). `complex_limit` is NOT supported by this wrangler → **simple mode with `period = 60`** (per-minute, matching the PROPOSED class limits). Period 10 is available but unused. |
| **S1b — one binding vs per-class limits** | `simple` mode applies ONE limit per namespace per key — per-class limits (PROPOSED 10/30/10/20 per min) cannot share one namespace. Correct shape: ONE `[[rate.limit]]` entry PER CLASS (auth/game/me/admin), each with its class's PROPOSED limit, all `period = 60`. Operator may provision a single namespace and point all four `namespace_id`s at it (uniform limit) — recorded in the handoff operator steps. Plan's singular "binding" wording is superseded by the verified toolchain constraint (recorded per prompt instruction). |
| **S1c — `cloudflare:rate-limit` import** | NOT a declared module in installed `@cloudflare/workers-types` (grep: no `module "cloudflare:rate-limit"`; only email/node/pipelines/workers/sockets/workflows are declared). The runtime shape is verified from the workers-types `RateLimit` interface: `limit({ key: string }) → Promise<{ success: boolean }>` (`experimental/index.d.ts:15362-15375`). Implementation uses a local structural `RateLimitBinding` type instead of the virtual import (no dependency on undeclared module). |
| **S1d — response headers** | The binding outcome exposes ONLY `success: boolean` — there is no limit/remaining/reset from the API. The middleware emits `x-ratelimit-limit` (class limit), `x-ratelimit-remaining: 0`, `x-ratelimit-reset` (epoch seconds ≈ now + period) synthesized from the class config, documented as such (plan F.4 "mirror what the binding returns; document exact names"). |
| **S1e — keying branches (plan F.3)** | Auth class: `CF-Connecting-IP` when present, else an explicit per-request `dev:<requestId>` no-op key (never a shared constant). Session classes (game/me/admin): `auth.user_id` — these limiters are mounted AFTER `requireAuth`/`requireAdmin`, so identity is always present; defensive null → same explicit dev key. No silent shared keying; every branch unit-tested. |
| **S1f — method skip** | `OPTIONS` and any method outside the class's unsafe set are skipped (no limiting): auth class = `POST` only (GET OAuth callbacks must not be throttled); game/me/admin classes = `POST|PUT|PATCH|DELETE` only (reads stay edge-limited per plan F.2, not app-limited). |
| **S1g — thresholds** | Class limits stay PROPOSED constants (plan §N D1), marked `PROPOSED/product-tunable`, in `src/server/middleware/rate-limit.ts`; the wrangler.toml values match them and are operator-tunable. |

### S2 CSP decisions (2026-09-02 — recorded before/while implementing, Phase-4 discipline)

| # | Decision | Resolution |
|---|---|---|
| **S2a — Kit `csp` option shape (plan [NV])** | Verified against installed kit 2.63 (`node_modules/@sveltejs/kit/types/index.d.ts:382-395`): `csp: { mode: 'hash'\|'nonce'\|'auto', directives: CspDirectives, reportOnly?: CspDirectives }`. Values are ARRAYS; there is no boolean reportOnly — dev report-only is expressed by passing the same directives under `reportOnly` when `CSP_REPORT_ONLY=1` (read at config load). Kit hashes only the inline scripts IT generates; the app.html pre-paint script needs its hash in `script-src` explicitly (doc note at :370-372 confirms nonce/hash augmentation only covers Kit-generated scripts). |
| **S2b — pre-paint hash** | Pinned literal `sha256-PBIDO3zx1vdOnPTvDJ3MOJX3bs7JGBpzpivzIRpKx3I=` in `csp.ts` (computed from the EXACT bytes of the `src/app.html` inline script). Pin test recomputes from the file and fails with the recomputed value in the message. |
| **S2c — `style-src` tightening (plan §G.2 "prefer strict")** | `'unsafe-inline'` REMOVED from `style-src` (now `'self'`), scoped to `style-src-attr 'unsafe-inline'` for the single pre-existing `style="display: contents"` attribute in app.html. Rationale: production styles are bundled (no runtime `<style>` injection); Svelte transitions mutate `element.style` programmatically (not CSP-blocked); Kit hash-mode covers its own inline styles. The e2e console-clean gate is the proof — if violations appear, fall back to blanket `style-src 'unsafe-inline'` (recorded fallback). |
| **S2d — serialization mirrors Kit** | `serializeCsp` quotes exactly the keyword set Kit quotes (`self`, `unsafe-*`, `none`, `strict-dynamic`, `report-sample`, `wasm-unsafe-eval`, `script` — `kit/src/runtime/server/page/csp.js:11-21`) so the API header and the page header render tokens identically; crypto (sha256-) and URL sources stay bare. |
| **S2e — G11 "same CSP value" nuance** | Literal page-vs-API equality is impossible in hash mode: Kit AUGMENTS `script-src` with computed hashes of its own inline bootstrap scripts on pages. Equality is enforced at the DIRECTIVE level: every directive from the shared builder appears identically in both headers, and the API value equals `serializeCsp(buildCspDirectives())` exactly (unit + curl probes record the diff = only Kit's extra script-src hashes). |
| **S2f — Kit report-only constraint** | Kit 2.63 REFUSES to start with `csp.reportOnly` unless `report-uri` or `report-to` is present (verified: dev server 500s with "must be specified with either the report-uri or report-uri directives"). Resolved: the DEV report-only directives include `report-uri: ['/__csp-report__']` even though NO endpoint exists (plan §G.3 — reports are read from devtools/e2e console; the browser's report POSTs 404 harmlessly in dev only). The production/enforced config never sets report-uri; the Hono API surface (no Kit constraint) keeps report-only without report-uri. |

### S3 test decisions (2026-09-02 — recorded, incl. one explicit re-scope)

| # | Decision | Resolution |
|---|---|---|
| **S3a — session-cookie flag pin (RE-SCOPED, plan §J "session-cookie flags on sign-in (set-cookie)")** | The app is Google-OIDC-only: there is NO local sign-in endpoint whose Set-Cookie is observable without a live Google round-trip; the social sign-in handler writes a DB verification row BEFORE emitting cookies (probed: inert DB → error, no Set-Cookie). Re-scope recorded: (1) e2e pins **HttpOnly at the browser** (`document.cookie` cannot read `better-auth.session_token` — security.spec); (2) unit pins the **cookie contract** — Better Auth 1.7.1 `createCookie()` defaults (`dist/cookies/index.mjs:27-42`: `httpOnly: true`, `sameSite: 'lax'`, `path: '/'`, `secure` only when production/https baseURL) + the app's `SESSION_COOKIE_NAME` constant + the signed-cookie format cross-checked against `@better-auth/utils/hmac` (the fixture's exact scheme — drift here would silently break every authenticated e2e); (3) Secure-on-https remains a Phase-6 production probe (https-only) with the source gate cited. |
| **S3b — security.spec scope** | One new file reusing the auth fixture: API bypass 401s, cross-user isolation (guess on foreign game → 403 FORBIDDEN, profile isolation), cross-site POST → 403 CSRF with a same-origin control, protected-page 307 redirects, non-admin 403 + redirect (E-A1 referenced, not duplicated), malformed/oversized → 400/413, sign-out invalidation (scenario-12 UI path referenced), HttpOnly e2e pin. CSP console-clean stays in csp.spec.ts (S2 decision — ONE home). |

### S6 dependency-automation decisions (2026-09-02 — recorded before code)

| # | Decision | Resolution |
|---|---|---|
| **S6a — audit tool (D5)** | **`bun audit`** (bun 1.4.0, installed) — verified: `--audit-level=high` filters HIGH/CRITICAL and exits 0 on the current tree ("No vulnerabilities found … 2 below --audit-level=high"). `--ignore <GHSA/numeric>` is the documented allowlist mechanism (no silent skips: every ignore entry carries a rationale comment in ci.yml). One scanner only (Architecture requirement). |
| **S6b — dependabot + bun.lock (D8, plan §K [NV])** | `npm` ecosystem covers `bun.lock` (GitHub Dependabot bun.lock support documented). Weekly schedule; `groups: development-dependencies` (dependency-type development, pattern `*`); `open-pull-requests-limit: 5`; security-updates remain GitHub-default ON with auto-merge OFF; `github-actions` ecosystem for the SHA-pinned actions. Fallback (only if GitHub rejects the config): monthly `bun update --latest` PR discipline recorded here — the config is validated at push (G14). |
| **S6c — Actions hardening (F10)** | Every `uses:` pinned to a full-length commit SHA with a `# vN` comment: `actions/checkout@fbc6f39…` (# v5), `oven-sh/setup-bun@0c5077e5…` (# v2) — resolved from the tags' commits via `git ls-remote`. Dependabot `github-actions` keeps the SHAs fresh. Workflow gains top-level `permissions: contents: read` (minimum). The integration `DATABASE_URL` mandatory gate and all parity assertions are untouched. |

### S5 ZAP triage + mitigations (2026-09-02 — decisions before code)

| # | Decision | Resolution |
|---|---|---|
| **S5a — ZAP run environment** | Docker daemon socket + podman user namespaces are BLOCKED in this sandbox (verified: docker socket permission denied; newuidmap EPERM). The dockerized script is delivered as the operator path (`scripts/zap/zap-baseline.sh`); the DOCUMENTED baseline run was executed with the SAME workflow via the official ZAP 2.17.0 standalone dist + the Automation Framework plan (spider → passiveScan-wait → reports) — same scanner, same passive rules. |
| **S5b — Medium finding "Cross-Domain Misconfiguration" (ACAO: `*`)** | **REJECTED as a preview artifact**: `vite preview` enables `preview.cors: true` BY DEFAULT (Vite docs) — every preview response (page + assets) carries `Access-Control-Allow-Origin: *`. Production responses carry NO ACAO: verified `curl` probes (page + API), zero ACAO in `src/` or the adapter `_headers` output. Mitigation: `preview: { cors: false }` in vite.config.ts so the LOCAL preview mirrors production (no product behavior change). |
| **S5c — Low "X-Content-Type-Options missing" on static assets** | Confirmed for `/_app/*` assets: the app-level nosniff contract covers pages + API; immutable assets are platform-served. Defense-in-depth via the deployable `_headers` file (`static/_headers`: `/_app/*` → nosniff — adapter-cloudflare merges user rules with its generated block). Note: `vite preview` does NOT apply `_headers` rules (Workers Static Assets feature) — the finding remains visible on re-scan of the PREVIEW but is mitigated in the deployed artifact. |
| **S5d — Informational "Suspicious Comments"** | **REJECTED**: the scanner matched substrings ('from', 'query') inside MINIFIED bundles — esbuild strips comments; not source leakage. |
| **S5e — scan limits** | The traditional spider reached 54 URLs on the SPA (landing + assets); API routes beyond the landing page are not crawl-discovered — the API attack matrix is covered by tests/e2e/security.spec.ts + unit suites (ZAP complements, per Architecture "scans are a starting point"). |

| **S5b-CORRECTION (2026-09-02)** | The ACAO source is **miniflare's preview emulation**, not vite's `preview.cors` (which is bypassed by the adapter's preview server): `Access-Control-Allow-Origin: *` appears on page, API, and asset responses under `vite preview`; `grep` of the DEPLOYED artifact shows **zero** ACAO in `_worker.js` and `src/`; miniflare's own asset-emulation worker (dist/src/workers/r2/public.worker.js) contains the CORS handling. `preview: { cors: false }` was tried and REVERTED (no effect under miniflare — misleading config). Reject stands with the artifact-level evidence. |

| **S6b-ACTIVATED (2026-09-02)** | First dependabot npm PR observed: **Dependabot's npm ecosystem does NOT update `bun.lock`** — the generated PR (`dependabot/npm_and_yarn/development-dependencies-9cbc5b4111`) changes `package.json` only (4 devDependencies: `@eslint/js`/`eslint` 9→10, `typescript` 6→7, `auth` 1.7.1→1.7.2). Verified: applying the PR's `package.json` locally, `bun install --frozen-lockfile` FAILS ("lockfile had changes, but lockfile is frozen") → the CI Install step of all three jobs would break. The documented §K FALLBACK is now ACTIVE: monthly `bun update --latest` maintenance PRs that touch package.json + bun.lock TOGETHER and pass the full gate sweep, plus the green `bun audit` step. The `auth` CLI pin (Phase-0: 1.7.1 for deterministic regeneration) must be bumped deliberately with an `auth:check` verification, not by the bot. The github-actions PR (checkout 5.1.0→7.0.1, full-SHA pinned) is the S6c flow working as designed and is safe to merge.
