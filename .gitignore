node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
.dev.vars

# Test output
coverage
playwright-report
test-results

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local tool caches (wrangler/miniflare XDG config redirected into workspace)
.cache/

# Private answer-pool seed inputs (NC3) — never commit
scripts/seed/*.txt

# Not needed markdown Files
/docs/gpt-replies/

# ZAP scan output (Phase-5 S5) — the TRIAGE table is the committed artifact
scripts/zap/reports/
